# 调研记录

## 用户已确认事实

- 当前两个打点都是 A 区水果点。
- 点1是 A 区左侧水果点，点2是 A 区右侧水果点。
- 首段 `585 mm` 来自场地 `500 mm` 加机械臂中心相对车头的 `85 mm` 偏移。
- 更正：A区共8个水果，沿行进方向4组、每组左右各1个；启动区边界到
  第一组和相邻组间距均为500 mm。85 mm只补偿启动时车头到机械臂中心，
  所以三次测试距离应为585/500/500 mm。
- 当前底盘实际运动方向已经重新验证。
- 后续会由上位机控制底盘，当前写死测试流程应降级为一个测试编排器，而不是底盘控制本体。

## 待核对

- PDF 中各区域、行进方向、得分/限制与放置要求。
- 当前代码中 `585/500` 的定义和调用位置。
- 左右安全点、旋转方向、释放位姿是否已经与 A 区假设绑定。
- 底盘距离、方向、IMU闭环和停车稳定的职责边界。

## 当前源码第一轮核对

- `app_config.h` 当前把首段定义为 `0.585f`，注释仅写“抓取循环开始前移动一次585mm”，尚未表达 `500 + 85` 的物理来源。
- `app_config.h` 当前把两次抓取间隔定义为 `0.500f`，与用户刚确认的后续每次 `85 mm` 不一致。
- `app_runtime.c` 直接使用上述两个距离宏推进写死测试编排，说明场地任务语义目前位于运行层，而不是独立的任务/路线描述层。
- 系统 PATH 中未找到 `pdfinfo`、`pdftotext`、`pdftoppm`，PDF 检查将改用可用的 Python PDF 库完成全文提取和页面渲染。

## 实施审计结论

- 已将底盘内部循环测试替换为单命令执行器；左右命令、反馈和IMU方向符号保持当前实测值不变。
- 底盘公共命令以mm为入口，正距离固定表示物理车头向前；状态为WAIT_READY/IDLE/RUNNING/STOPPING/COMPLETED/CANCELLED/FAULT。
- 放置流程已改为完整profile输入，安全、释放、恢复均显式包含q1/q2/q3，源码不再根据抓取q1正负选择路线。
- A区任务表固定为585/A左、500/A右、500/A左；B/C/D没有假参数和A区回退入口。
- `undefined/` 已确认仅含本轮Node缓存，但精确的cmd和PowerShell删除均被执行策略拒绝，目录暂时保持未跟踪。
