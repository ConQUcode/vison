# Progress

- Phase 51 started: user requires upper-controller task 6 HOME to match the proven full-boot order. Source audit confirms the required sequence is cancel current motion, q2+q3 together, q1, then ID1/ID2; the current task 6 implementation combines the three DM axes and ID1 in one command.
- Phase 51 design audit confirmed ordinary joint trajectories preserve the current ID1 relative angle, so the two DM-axis stages will not drive ID1 through absolute-pitch compensation. The final tool stages will explicitly command ID1 neutral relative pitch and ID2 READY, with reset-stage diagnostics retained.
- Phase 51 implementation split task 6 into cancel, q2+q3, q1, ID1 and ID2 asynchronous submit/wait stages. Every stage advances only on its own command ID completing with OK; failure snapshots now include the reset state and completed-stage bitmask.
- Phase 51 initial validation passed strict Cortex-M4 GCC with `-Werror`, the complete HOST path replay and `git diff --check` (line-ending notices only). `Engineer.uvoptx` remains byte-identical; Keil full rebuild is pending.
- Phase 51 complete. Keil ArmCC 5.06u7 full rebuild produced AXF/HEX/MAP with 0 errors and 0 warnings; final size is Code=143924, RO-data=3660, RW-data=1432 and ZI-data=137560. `Engineer.uvoptx` SHA-256 remains `6ae31918e053173831440f0220423ff8d652ee822188d32c34f9ddc02a018fcb`. Firmware was not flashed and no live task 6 or physical-arm test was performed.

- 2026-08-19：开始阶段49。按批准计划只实施赛前第一阶段，目标是让AC推进降级、正式轨迹和HOST回放共用生产路径预检规则。
- 2026-08-19：已确认核心FK/IK不是主要风险；当前桥层贪心预检缺少生产轨迹使用的多候选动态规划、连续性和工作区检查。
- 2026-08-19：首次跨三个历史记录文件追加因`findings.md`结构不匹配被原子拒绝，未修改文件；已改用各文件稳定标题分别追加。
- 2026-08-19：已完整提取生产单段规划的数据流：两遍IK、全候选动态规划、前驱回溯、候选重生成和CCM轨迹写入；下一步建立共享段规划接口。
- 2026-08-19：已新增无硬件副作用的`arm_path_planner.c/.h`及显式请求/工作区/诊断结构，并把相邻IK样本关节步长常量提升到共享配置；尚未接入生产轨迹。
- 2026-08-19：正式工具中心单段预检已切换到共享规划器，并保持原CCM缓存、ID1服务频率、轨迹持续时间和旧Watch映射；尚未编译。
- 2026-08-19：首次严格GCC检查从错误工作目录启动，脚本相对路径找不到`tim.c`；未形成源码编译结论，后续改从MDK-ARM目录运行。
- 2026-08-19：首次正确ARM GCC编译到共享模块后，在轨迹适配处发现`ARM_JOINT_COUNT=4`与主臂三轴缓存不兼容；将规划接口固定为三自由度数组后重试。
- 2026-08-19：共享模块与生产轨迹通过ARM严格GCC；旧HOST回放链接时发现工具几何shim缺少生产规划器所需的俯仰合法性函数，正在补等价纯计算实现。

- 2026-08-16：开始阶段23。诊断第一段仅转底座导致工具中心Y从0上升到约326.5mm；将搜索并验证Y全程不超过260mm的同步臂关节过渡姿态。
- 2026-08-16：源码确认第一段只覆盖q1；锁定最小改动为同步设置过渡q2/q3/ID1并保留第二段工具中心直线状态机。

- 2026-08-16：开始阶段22。将默认固件从打点模式切换为BD左侧观察点执行模式，目标参数保持阶段21验证值不变。
- 2026-08-16：阶段22模式/点位复核、`git diff --check`和ARM GCC严格检查通过，准备Keil全量重建BD执行固件。
- 2026-08-16：阶段22完成。Keil全量重建BD执行固件为0错误0警告，AXF/HEX/MAP于22:01更新；未烧录、未执行机械臂实测。

- 2026-08-16：开始阶段21。目标为BD左侧观察点`[0,57,210] mm`、绝对俯仰`-30deg`、底座朝左；保持`APP_MODE_ARM_TEACH_POINT`，先做实时源码与完整轨迹校验。
- 2026-08-16：已核对实时默认模式、旧BD参数、朝左`q1=+90deg`预对准及打点模式执行边界；确认轨迹层已有全候选动态规划用于径向过零分支连续性。
- 2026-08-16：已确认BD执行顺序为HOME后先底座朝左、再从真实反馈姿态提交带绝对俯仰的工具中心直线命令；本轮不改该状态机。
- 2026-08-16：选定现有`tools/arm_path_replay`作为源码等价的完整路径验证入口，拟临时加入BD用例、运行后撤掉临时代码。
- 2026-08-16：锁定BD直线路径的理论起点为`q=[90,90,-60]deg`、绝对俯仰`-30deg`，并核对实时关节/ID1/相邻采样限制。
- 2026-08-16：临时BD完整路径回放通过，282点、终点`q=[90.000,168.151,-47.928]deg`；继续提取全程关节、ID1和单步余量。
- 2026-08-16：完整路径余量通过；撤销临时回放代码并删除专用CSV，正式把BD参数改为`[0,57,210]mm/-30deg`，保持`q1=+90deg`、`150mm/s`和打点模式。
- 2026-08-16：正式配置/模式复核、`git diff --check`和ARM GCC严格检查均通过；待执行Keil打点固件全量重建。
- 2026-08-16：首次Keil调用参数与当天成功方式不一致并等待无进展；已只终止本次子进程，定位正确方式为`MDK-ARM/tmp/-cr/-j0`并准备重试。
- 2026-08-16：阶段21完成。正确参数下Keil全量重建为0错误0警告，AXF/HEX/MAP于21:55更新；最终仍为打点固件，未烧录、未执行BD机械臂运动。

## 2026-08-14

- Started source audit for the `-480` one-shot pick/place test and shoulder-first power-on initialization.
- Confirmed live test baseline and preserved existing dirty worktree changes.
- Confirmed posture mode currently stops at `HOLDING`; identified the need for a shared A-left place-profile accessor and explicit grip-only states before starting `AppArmFlowStartPlace()`.
- Confirmed active full-init currently initializes ID1/ID2 first and moves all three DM joints together; selected a shoulder-first q2 -> q3 -> q1 sequence with deferred tool initialization.
- Identified the posture-mode link guard and direct ID2 close API; selected a shared profile accessor followed by `AppArmFlowStartPlace()`.
- Verified existing single-axis motion helpers correctly preserve logical q1/q3 while q2 moves and retain timeout/wrong-direction protection; verified deferred Huaner task servicing is a no-op before driver init.
- Added a shared A-region place-profile accessor and broadened `AppArmFlow` implementation linkage to full-arm and posture-test modes.
- Added the one-shot posture states for `-480` pitch settling, ID2 close, A-left place, and final `HOLDING`; current source still needs startup sequencing and static review.
- Re-read the live full-init path and locked the implementation mapping: q2 uses `MOVE_AXIS/WAIT_AXIS`, q3 and q1 use `MOVE_SAFE/WAIT_SAFE`, then the boot state starts ID1/ID2 initialization once.
- Implemented `ArmToolPrepareDeferredInit()` and changed full-init startup to q2 shoulder -> q3 elbow -> q1 base -> ID1/ID2, reusing the existing single-axis limit, timeout, wrong-direction, and coupling behavior.
- Removed the now-unused all-axis arrival helper after sequential auto-init replaced the simultaneous HOME command.
- Corrected the auto-init Watch axis encoding and updated README/HANDOFF/overview/tuning/fruit-flow documentation for `-480`, one-shot close/place, q2-first startup, and the live q2 `10..180 deg` soft range.
- Completed static validation: no stale `-460`/no-grip text, startup call order and posture states are present, the 141-point source-equivalent path scan passes, and `git diff --check` reports no whitespace errors.
- Per user constraint, did not run Keil/GCC compilation, flashing, or hardware motion tests.

## 2026-08-15

- Confirmed the post-grip stop is `ARM_COMMAND_DUPLICATE`: posture mode ends at `0xA11C0003`, then the first place command regresses to `0xA11B0001`.
- Started phase 6 to replace independent application arm command-ID ranges with one shared monotonic allocator.
- `session-catchup.py` failed because MSYS Python could not determine HOME; continued from the existing planning files and live source state.
- Audited all application command producers and selected a standalone shared arm command-ID module; chassis IDs remain intentionally separate.
- Completed interface/integration design: atomic idempotent allocator, submit-time posture IDs, shared flow IDs, Keil APPLICATION group entry, and compact Watch state.
- Added `app_arm_command_id.c/.h`, migrated all posture and `AppArmFlow` arm IDs, removed both private bases/sequences, and initialized the allocator from the common arm `AppInit()` path.
- Added the allocator source to the Keil APPLICATION group; XML parsing succeeds and finds exactly one project entry.
- Static search confirms only the intentionally separate chassis command-ID base remains.
- Documented the shared allocator ownership rule and `g_app_arm_command_id_debug` in PROJECT_OVERVIEW, HANDOFF, and TUNING_GUIDE.
- Rechecked every application-layer `ArmSubmit*` call: posture commands retain one allocated ID across BUSY/NOT_READY retries, and every `AppArmFlow` submission obtains its ID from the common allocator.
- Verified the mailbox accepts any first non-zero ID while its latest ID is zero, so the high-bit seed is valid; later IDs use signed modular ordering and the allocator skips zero on wrap.
- Final static validation passed: no old arm ID bases/private generator remain, Keil XML parses with exactly one allocator source entry, and `git diff --check` reports no whitespace errors.
- Per the locked boundary, did not run Keil/GCC compilation, flashing, or hardware motion tests; the modified `Engineer.uvoptx` remains an existing user change and was not edited for this fix.
- User build exposed `L6218E` for `AppArmCommandIdInit/Next`; live `Engineer.uvprojx` no longer contained the allocator source, consistent with an open/stale Keil project overwriting the earlier external XML edit.
- Re-added `app_arm_command_id.c` to the active `Engineer` target and ran a Keil incremental build with project-local `TEMP/TMP`: the source compiled, `Engineer.lnp` contains `app_arm_command_id.o`, the map resolves both symbols from that object, and the final image reports 0 errors and 0 warnings. No flashing or hardware motion was performed.
- Started phase 7: verify a `-5 deg` absolute side-gripper pitch over the full `Y=-340..-480 mm` path, then change full-init from q2 -> q3 -> q1 -> tool to q2+q3 -> q1 -> tool without weakening startup fault protection.
- Full-path source-equivalent scan rejected `-5 deg` at `[0,-340,-135]` (`relative_pitch about +90.834 deg`, servo position about `121.53`, below the 125 limit), so the conditional pitch change was not applied; current `-10 deg` remained at that phase.
- Implemented a coupled q2/q3 HOME command with independent arrival-stability and wrong-direction tracking for both axes, shared timeout, and preserved feedback/motor/hard-boundary/CAN fault latching; q1 remains the second DM stage and ID1/ID2 remain deferred until both DM stages complete.
- Keil incremental build compiled the synchronized initializer and linked `Engineer.axf` with 0 errors and 0 warnings; no flashing or hardware motion was performed.
- Updated README, PROJECT_OVERVIEW, HANDOFF, TUNING_GUIDE, electrical/lower-controller documentation, and arm configuration comments for q2+q3 -> q1 -> tool startup semantics and the rejected `-5 deg` pitch boundary.
- Final phase-7 validation passed: no stale sequential q2 -> q3 startup description or auto-init step 3 remains, `APP_ARM_POSTURE_TEST_TOOL_PITCH_DEG` is still `-10.0f`, project XML remains valid with one command-ID source entry, and `git diff --check` reports no whitespace errors.
- Changed the posture-test absolute pitch to `-7.0f` after a source-equivalent 1 mm scan passed the full path with about `1.538 deg` minimum combined margin; synchronized q2/q3 initialization remains unchanged.
- Started phase 9/10 after hardware testing showed intermittent post-release lockup and visible Z rise during the side push.
- Decoded the captured fault as an ID2 release-open feedback dropout: communication later recovered near target, but the tool and place-flow failure latches correctly explained why no return motion followed.
- Confirmed the live ID1 conversion direction is negative; older planning/docs control-position numbers are mirrored even though their relative-angle feasibility conclusion is unchanged.
- Implemented bounded ID2 feedback recovery for READY/OPEN motions: short dropouts no longer latch an immediate fault, recovered feedback must still prove arrival, and sustained loss or the existing total action deadline still faults explicitly.
- Added `gripper_feedback_lost_tick`, dropout count, and recovery count to `g_arm_tool_debug` for hardware Watch diagnosis.
- Recomputed the current `-7 deg`, `Y=-340..-480 mm`, `z=-135 mm` path from the live geometry: q2 is the first lower-Z constraint under the old 10 deg limit, so the normal q2 limit was reduced to 3 deg while the 0 deg escape/hardware boundary remains unchanged.
- Confirmed that after the q2 change the next mathematical lower-Z boundary is ID1 relative pitch at about `z=-162.466 mm`; current coordinates remain unchanged at `z=-135 mm`.
- Corrected the mirrored ID1 control-position values in the planning and tuning documentation and synchronized the q2 range and ID2 recovery behavior in the electrical/tuning documents.
- The 141-point current-path scan passes after the q2 change: q2 minimum is about `11.794 deg`, q3 remains `-103.080..-67.524 deg`, and ID1 relative pitch remains `58.126..88.462 deg`.
- `gcc_arm_check.cmd` passed all configured Cortex-M4 `-Wall -Wextra -Wshadow -Werror` syntax checks, including `arm_tool.c`, with no errors.
- Two native Keil `UV4.exe` incremental-build invocations returned without starting a build or producing a log; existing object/image timestamps did not change, so no Keil result is claimed. `.uvprojx/.uvoptx` hashes were identical before and after.
- Final static checks report no whitespace errors. No flashing or hardware motion verification was performed.
- Started phase 11 after the user confirmed ID1 hardware can accept control position115; synchronized the ID1 minimum position to115 and relative-pitch maximum to the equivalent `+92.4 deg`, including the tool startup self-test boundary.
- Changed the active posture-test path to `[0,-340,-150] -> [0,-480,-150] mm` with absolute pitch `-5 deg` and synchronized all current-mode documentation.
- Started phase 12: mirror the complete world-Y coordinate definition so physical left is `+Y`, while preserving point identities, q1 signs and physical pick/place routes.
- Completed phase 12: mirrored the full Y conversion chain and current documentation. The host replay passed all 141 points from `[0,340,-150]` through `[0,480,-150]` with minimum q2 `7.881 deg`, maximum ID1 relative pitch `91.706 deg` and minimum ID1 position `117.893`; A-left/A-right remained q1 `+90/-90 deg`.
- Expanded `gcc_arm_check.cmd` to include the application flow, fruit task and shared command-ID sources; all eight checked translation units passed ARM GCC syntax validation. No firmware was flashed and no bench motion was performed.
- The source-equivalent 141-point scan passes with minimum ID1 position about117.893, maximum relative pitch about91.706deg, minimum q2 about7.881deg, and q3 within `-106.155..-70.762deg`.
- Phase 11 final validation passed: current macros and tool self-test use the synchronized115/+92.4deg boundary, strict Cortex-M4 GCC syntax checks passed, no stale current-mode `-7deg/-135mm` documentation remains, and `git diff --check` reports no whitespace errors. Keil compilation, flashing, and hardware motion were not performed.
- Started phase 13 for an indefinite left/right mirrored pick-place loop in posture-test mode; preserved the current no-chassis test boundary and all existing user changes.
- Added structural left/right Y macros, an explicit per-side posture-cycle preparation helper, mirrored profile selection, fresh command IDs per cycle, and Watch counters for total/left/right completed picks.
- Replaced terminal post-place holding with left/right side switching after the arm has returned to the front; the failure state remains terminal.
- Extended the host replay verifier to compare all 141 left/right path samples for q1 sign symmetry, identical q2/q3, identical ID1 relative pitch/control value, and equal automatic-workspace acceptance.
- ARM GCC strict syntax validation passed all eight configured arm/application translation units. The first host replay build caught only a shadowed test variable; after renaming it, the full mirrored-path and existing rear-route replay passed.
- Updated the top-level README and handoff current-mode section to describe the indefinite left-first/right-second loop and both coordinate paths.
- Updated PROJECT_OVERVIEW, FRUIT_TASK_FLOW and TUNING_GUIDE so the active mode, profile selection, mirrored coordinates, failure behavior and symmetric limit margins match the new runtime loop.
- Updated the electrical/lower-controller guide for paired left/right path validation and removed its final stale one-shot-mode description.
- Re-reviewed the cycle boundary and tightened counter ordering so an invalid active-side value cannot be counted; command IDs are cleared per side and allocated only at their normal submit states.
- Removed the now-unreachable successful `HOLDING` posture state; normal operation always prepares the opposite side after a completed return-to-front, while `FAILED` remains the only terminal posture state.
- Final stale-text audit found no remaining one-shot/HOLDING current-mode behavior in application source or maintained documents.
- Completed phase 13: left-first/right-second indefinite mirrored pick-place is implemented, symmetric software-limit replay and strict ARM syntax checks pass, and all maintained current-mode documents are synchronized. No firmware was flashed and no hardware motion was performed.
- Started phase 14 read-only diagnosis after hardware testing showed the right cycle stopping immediately after base pre-aim; no motion parameters have been changed.
- Completed phase 14 diagnosis: the right target IK is valid, but the first Cartesian sample from the left-release return pose fails only the front-barrier workspace check (`X=2.011 mm`, `q2=120.031 deg`), proving a preflight rejection before DM shoulder/elbow motion.
- Started phase 15 to make posture-test pre-aim enter the same q2/q3/ID1 staging posture as the formal pick flow on every left/right cycle, without relaxing any safety limit.
- `session-catchup.py` again failed because MSYS Python cannot determine HOME; continued from the live plan/findings/progress files and current Git state, and will not retry this known environment failure.
- Confirmed the joint-command API and trajectory layer already support synchronized DM q1/q2/q3 plus ID1 relative pitch with full-path safety validation; selected a shared command builder so formal and posture-test pre-aim cannot diverge again.
- Added `AppArmFlowBuildPickStaging()` and migrated both formal pick and posture-test pre-aim to it; posture mode now submits all three DM staging targets and ID1 relative pitch in one command instead of preserving release q2/q3.
- Extended `arm_path_replay` with real post-place-to-staging and staging-to-pick transition checks; both sides and the existing rear-route cases pass.
- First strict GCC invocation was made from the repository root and could not resolve the script's relative `../APPLICATION` paths; no source diagnostic was produced, so validation will be rerun from `Engineer/MDK-ARM`.
- Reran strict ARM GCC validation from `Engineer/MDK-ARM`; all eight configured translation units passed with warnings treated as errors.
- First combined documentation patch found a stale expected context in `HANDOFF.md` and was rejected atomically; confirmed no document changed, then switched to file-scoped patches.
- A second still-cross-file attempt was also rejected by `HANDOFF.md`; no document changed. Stopped combined patches and moved to independent one-anchor edits per file.
- Updated README and maintained arm/task/tuning/electrical documents with the shared per-side staging boundary and current verification scope.
- Final validation passed: ARM GCC strict check, both mirrored post-place staging/approach/advance replays, existing rear-route replay, symbol audit and `git diff --check`. Removed only the four newly generated ignored staging-candidate CSV files; no Keil build, flash or hardware motion test was performed.
- Final display-only searches with spaced patterns were split by native cmd; switched to simple symbol lookups for line references. No implementation or verification result was affected.
- A final display-only `findstr` status lookup had the same cmd quoting limitation; stopped repeating it. The phase-complete edit itself had already applied successfully.
- Completed phase 15.
- Started phase 16 to extract one complete side pick/place operation behind a reusable non-blocking LEFT/RIGHT command API while preserving the current automatic alternating test and leaving USB protocol unchanged.
- Phase 16 inspection confirmed the verified state machine can be migrated without changing any motion parameter. Locked the public boundary as one non-blocking `Start(side)` plus periodic `Poll()`, with `DONE` after exactly one side and automatic alternation retained only in `app_runtime.c`.
- Added `app_arm_side_pick_place.c/.h`, moved the complete verified single-side state machine and legacy Watch symbol into it, and reduced posture-test runtime code to a LEFT/RIGHT alternating caller. Added the new source once to the Keil APPLICATION group and strict GCC syntax-check list without modifying `.uvoptx`.
- Strict ARM GCC syntax validation passed all nine configured translation units, including the new module and runtime caller. Mirrored path replay and rear-route replay passed unchanged; `git diff --check` found no whitespace errors.
- Synchronized README, HANDOFF, PROJECT_OVERVIEW, FRUIT_TASK_FLOW and TUNING_GUIDE with the reusable LEFT/RIGHT Start/Poll contract and future protocol-bridge boundary. Kept the legacy posture Watch symbol and the numeric value of its existing FAILED state stable; appended new status fields instead of reordering old fields.
- Completed phase 16. Final strict ARM GCC check, mirrored/rear path replay, single Keil source-entry check, ownership searches and `git diff --check` all passed. No Keil build, flash, or bench motion test was performed in this phase.
- Started phase 17 for a temporary MG995-only image: TIM8_CH2/PI6 right servo and TIM8_CH3/PI7 left servo, both commanded to 90deg, with arm/chassis/IMU application paths disabled by mode selection.
- Confirmed the user's CubeMX generation already supplies a correct TIM8/PI6/PI7 peripheral path. Scoped code changes to the MG995 wrapper, application-mode selection, motor-task suppression, Keil source integration and documentation; generated core settings will not be rewritten.
- Added the MG995 PWM wrapper with explicit right-CH2/PI6 and left-CH3/PI7 ownership, 1000..2000us angle mapping, 1500us startup targets, TIM8 configuration validation and compact Watch state. Switched the current application mode to MG995-only and suppressed all DM/DJI periodic control in that mode.
- Updated current-mode documentation for the MG995-only image while retaining the verified arm flow as a disabled mode. Expanded the strict GCC check to include the CubeMX TIM/GPIO/ISR/main path as well as the new driver and application code.
- The first expanded GCC pass confirmed TIM/GPIO/ISR, then stopped only because `main.c` includes the ArmCC-specific RVDS FreeRTOS port. Removed that incompatible cross-compiler check target and selected a native Keil build for the complete image.
- Strict GCC validation then passed all selected generated/driver/application units. The first native Keil attempt hit the host's known ARMCC temp-name failure and explicitly produced no target; selected the proven target-local `Engineer/tmp` plus `env.exe` full-rebuild path instead of treating the false 0-error footer as success.
- Completed phase 17. A full Keil rebuild using `MDK-ARM/Engineer/tmp` finished with 0 errors and 0 warnings and produced fresh AXF/HEX/MAP artifacts containing the MG995 driver. Map-level ownership confirms only MG995 initialization remains active and every arm/chassis/IMU/USB/motor application task is a no-op. No firmware was flashed and no physical servo motion was observed by Codex.
- Started phase 18 to add a firmware-internal continuous chassis `vx/wz` command boundary without modifying the generated USB protocol or current MG995-only application mode.
- Confirmed the live differential core, M3508 speed/current loops and IMU heading PID can be reused; the missing pieces are a refreshable velocity command type, timeout stop policy, zero-`wz` heading capture and proportional combined wheel limiting.
- Audited the live start/stop helpers and selected reuse of RUNNING/STOPPING/CANCELLED for velocity streaming. The current GCC helper omits chassis.c and will be extended as part of validation.
- Added the public BODY_VELOCITY command type, `Chassis_Velocity_Command_s`, refreshable `ChassisSubmitVelocityCommand()` entrypoint, status/Watch fields and bounded velocity configuration.
- Implemented zero-wz IMU heading capture/hold, direct nonzero-wz control, linear/angular acceleration ramps, proportional combined wheel limiting and 300ms refresh-timeout transition to ramped CANCELLED stopping.
- Added chassis.c to the strict GCC helper before running validation.
- Strict Cortex-M4 GCC validation passed with chassis.c included and warnings treated as errors; `git diff --check` reported no whitespace errors beyond existing line-ending notices.
- Self-reviewed start, refresh, direct-turn-to-heading-hold, timeout and cancel transitions; no separate velocity state is required and existing relative-command terminal behavior remains intact.
- Updated HANDOFF, PROJECT_OVERVIEW, TUNING_GUIDE, FRUIT_TASK_FLOW and electrical/lower-controller documentation with the internal velocity API, units, IMU zero-wz behavior, timeout, limits, Watch fields and explicit no-protocol boundary.
- Tightened status semantics so timeout/cancel immediately publishes zero vx/wz targets while the internal ramp decelerates from the current command; initialized the wheel-pair scale Watch to 1.0.
- Final static audit confirmed the generated protocol still has only FruitDetection/ACK/Heartbeat/Handshake and hash `0x0EBAB184`; current app mode remains MG995-only.
- Full Keil rebuild with project-local TEMP/TMP compiled chassis.c and completed with 0 errors and 0 warnings. Fresh AXF/HEX/MAP/chassis.o were generated at 21:39; map removal of the unused velocity API is expected in the active no-chassis MG995 image.
- Completed phase 18. No protocol packet was added, no firmware was flashed, and no chassis hardware motion was performed.
- Final plan helper could not parse the repository's legacy `[complete]` phase markers and reported 18/0; logged this tooling-format limitation without rewriting the existing plan history. Source/build completion evidence is unaffected.
- Started phase 19 after the user replaced the three generated upper-controller protocol files.
- Audited the generated delta and existing protocol runtime, USB port, fruit bridge, chassis velocity API, MG995 interface, application mode gates, and Keil source entries.
- Locked the integration boundary: generated files remain the wire-format source of truth; add a separate firmware bridge and host-control mode, preserve current MG995-only active mode, and defer all ArmTarget motion until camera extrinsics/capture pose are defined.
- Added the protocol-policy adaptation, upper-controller bridge, host-control app mode, Keil source entry, and expanded strict syntax check. The first host-mode-specific pass found only an unused refreshed timestamp in `AppArmTask`; patched that isolated conditional branch.
- Strict ARM GCC checks passed all generated protocol, runtime, bridge, chassis, arm, servo and application translation units, including a second `app_runtime.c` pass forced to host-control mode.
- Full Keil rebuilds passed with ArmCC 5.06u7 at 0 errors and 0 warnings in both host-control mode and the restored MG995 default mode. The host map proved the velocity callback reaches `ChassisSubmitVelocityCommand` and the bridge Init/Task remain linked.
- Updated README, HANDOFF, PROJECT_OVERVIEW, TUNING_GUIDE and FRUIT_TASK_FLOW to the new hash, packet set, runtime policy, bridge behavior, ArmTarget deferral and validation boundary.
- Completed phase 19. Final AXF/HEX are MG995-mode artifacts; no firmware was flashed and no hardware protocol or motion test was performed.
- Started phase 20 to implement the complete camera-frame target transform, calibrated extrinsic boundary, capture-time arm pose snapshot and offline math verification while keeping ArmTarget motion disabled.
- Added a hardware-independent `T_B_E * T_E_C * P_C` module with explicit wrist/tool reference frames, direct matrix and RPY configuration, orthogonality/determinant checks, capture-ID/age guards and compact Watch state.
- Integrated ArmTarget with the transform observation path and added `UpperControllerCaptureCameraPose()` for the future image trigger; no transform result calls an arm motion API, and the default calibration flag remains zero.
- All 29 offline transform checks passed, including X/Y/Z rotations and left/right symmetry. Strict ARM GCC checks passed every configured unit.
- Full Keil rebuilds passed with 0 errors and 0 warnings in temporary host-control mode and restored MG995 mode. The host MAP links `on_receive_ArmTarget` to `CameraTargetTransformLatest`; final AXF/HEX are MG995 mode. No flashing or camera/arm hardware validation was performed.
- Completed phase 20.
- Phase 23继续：复核关节轨迹和ID1插值语义，确认可用单个同步q1/q2/q3目标实现底座与小臂协同；开始离线搜索满足工具中心Y上限且可连续进入BD目标的过渡姿态。
- Phase 23实现：BD第一段改为同步目标`q=[90,120,-48]deg`并由ID1保持`-18deg`相对俯仰；现有回放工具新增两段逐采样Y上限和完整IK连续性检查，临时候选搜索源码已删除。
- Phase 23验证完成：BD两段回放最大`|Y|=231.470mm < 260mm`、最终解`q=[90.000,168.151,-47.928]deg`；原AC回放、ARM GCC严格检查和`git diff --check`均通过。
- Keil全量重建`arm_bd_coordinated_y260_rebuild_20260816.log`通过，0错误0警告，新AXF/HEX/MAP于22:20生成；未烧录、未做机械臂实机验证。Phase 23完成。
- Phase 24开始：按用户要求把默认BD观测从左侧镜像到右侧，计划只修改BD方向参数和当前模式文档，复用现有状态机、轨迹安全层与回放约束。
- Phase 24实现并验证完成：默认BD方向改为`q1=-90deg`、目标`[0,-57,210]mm`；回放最大`|Y|=231.470mm`、最终解`[-90.000,168.151,-47.928]deg`，原AC回放与ARM GCC严格检查通过。
- Keil全量重建`arm_bd_right_observation_y260_rebuild_20260816.log`通过，0错误0警告，新AXF/HEX/MAP于22:31生成；未烧录、未做机械臂实机验证。Phase 24完成。
- Phase 25开始：把BD左右观察点整理为显式双点配置，当前选择RIGHT；计划扩展回放为每次同时验证LEFT/RIGHT，并重建可上传固件。
- Phase 25实现完成：LEFT/RIGHT观察点、q1方向和用途均在`app_config.h`显式注释，状态机通过ACTIVE_SIDE别名读取当前RIGHT；回放改为每次独立验证两侧。
- 双侧回放、ARM GCC严格检查和`git diff --check`通过；Keil全量重建`arm_bd_dual_points_active_right_rebuild_20260816.log`为0错误0警告，新AXF/HEX/MAP于22:41生成。未烧录，Phase 25完成。
- Phase 26开始：核对`VelocityCommand -> UpperControllerBridge -> ChassisSubmitVelocityCommand -> ChassisTask`完整路径、单位、限幅和300ms超时后，准备将默认模式切换为现有`APP_MODE_HOST_CONTROL`。
- Phase 26实现完成：默认模式切到`APP_MODE_HOST_CONTROL`，保留BD左右观察点和AC流程；当前上位机速度范围为`linear_x +/-0.2m/s`、`angular_z +/-0.8rad/s`，建议至少10Hz刷新，300ms超时平滑停车。
- 路径回放、ARM GCC严格检查和`git diff --check`通过；Keil全量重建`host_control_chassis_rebuild_20260816.log`为0错误0警告，新AXF/HEX/MAP于23:01生成，MAP确认VelocityCommand到底盘提交链和Bridge Init/Task。未烧录、未做USB/底盘实机验证，Phase 26完成。
- 2026-08-17：Phase 27开始。确认当前超限有限速度会被`ChassisVelocityCommandValid()`整包拒绝；计划改为线速度`1.0m/s`、角速度`1.5rad/s`、单轮`1.3m/s`及配套加速度，并对超限值逐字段饱和。
- 2026-08-17：Phase 27源码实现完成。有限超限速度改为逐字段钳位并保持ACCEPTED，新增原始请求值和线/角/总钳位计数Watch；HOST速度、单轮速度和加速度边界已配套提高，ARM GCC严格检查通过。
- 2026-08-17：Phase 27完成。BD/AC回放、`git diff --check`和Keil全量重建通过，0错误0警告，新AXF/HEX/MAP于01:14生成；未烧录、未做1m/s底盘实测。
- 2026-08-17：Phase 28开始。确认新`task_id=2`无需改协议结构，但当前桥会直接拒绝且HOST编译到AC单侧任务stub；计划补齐HOST真实状态机、机械臂任务轮询和左/右/双侧串行回调调度。
- 2026-08-17：Phase 28实现完成。HOST启用真实AppArmFlow/单侧抓放状态机，ArmTask唯一Poll；桥支持task_id 2的左、右、左后右串行调度及callback_id 2开始/最终完成回调，协议和维护文档已同步。
- 2026-08-17：路径回放、HOST GCC严格检查、`git diff --check`及Keil全量重建通过；最终构建0错误0警告，新AXF/HEX/MAP于01:50生成，MAP确认AC Start/Poll/ExecutionCallback完整链。未烧录、未做USB/底盘/机械臂实机验证，Phase 28完成。
- Phase 29 started: audited the three user-updated generated protocol files and confirmed `PROTOCOL_HASH=0x740E426B`, `FruitDetection` removal, AC callback ID 2 preservation and ArmTarget callback ID 3 reservation.
- Removed the obsolete FruitDetection maintenance bridge from `protocol_runtime`, application initialization/polling, GCC syntax-check inputs and Keil project membership; deleted the now-unbuildable `fruit_usb_bridge.c/.h` files.
- Phase 29 static verification passed: both arm-path replay suites passed, `gcc_arm_check.cmd` passed all sources plus the explicit HOST-mode compile, and `git diff --check` reported no whitespace errors.
- The only remaining `fruit_usb_bridge` text under `Engineer/` is stale per-user IDE state in `Engineer.uvoptx`; it is intentionally untouched and is not a compiled `.uvprojx` member.
- Keil full rebuild `host_control_protocol_740e426b_rebuild_20260817.log` completed with 0 errors and 0 warnings; new AXF/HEX/MAP were generated at 02:58 and the MAP confirmed all required protocol call paths while excluding FruitDetection/fruit_usb_bridge.
- Restored the Keil-generated `Engineer.uvoptx` side effect to its exact pre-build state; Phase 29 is complete. Firmware has not been flashed and no live USB, chassis or arm hardware test was performed.
- 2026-08-17：Phase 30开始。目标是在不修改全局工作空间边界的前提下，为AC抓取终点到放置准备姿态增加左右镜像的受约束过渡，并在离线回放中逐采样证明夹爪中心`|Y|<=430mm`。
- Phase 30临时搜索器已建立；首次编译只因本机GCC默认临时目录落到`C:\\WINDOWS`而失败，尚未执行搜索，下一次改用工具本地`_tmp`目录。
- Phase 30搜索器已运行：当前直达safe_q的真实峰值为`479.161mm`；带单一关节waypoint的可行候选充足，正在从保留关节余量的候选中确定正式镜像参数。
- Phase 30正式路径已接入A区放置profile，waypoint为`[+/-90,20,-45]deg`；永久回放验证AC最大`|Y|=420.000mm`且左右严格镜像，原BD/AC循环/后方路径测试继续通过。临时搜索源码已删除。
- Phase 30严格ARM GCC检查通过全部配置单元和HOST模式，profile字段引用审计及`git diff --check`通过；待同步维护文档并决定是否执行Keil重建。
- Phase 30追加运行时硬预检：A区profile携带430mm上限，每次按实际q/ID1反馈逐1deg验证两段，超限不提交运动；严格GCC、全套回放和diff检查再次通过。
- Phase 30完成。最终AC规划峰值`420.000mm`，运行时上限`430mm`；左右镜像、原有路径回放、严格GCC和diff检查均通过。未做Keil重建、烧录或机械臂实机运动验证，且未触碰`Engineer.uvoptx`。
- 2026-08-17：Phase 31开始。用户要求AC终点恢复`Y=+/-440mm`、接近和终点Z提高到`-140mm`，抓后向内收时同步抬高约20mm，完整放置准备上限改为`|Y|<=440mm`；将从新端点重新求解并验证镜像waypoint。
- Phase 31临时搜索完成第一轮：旧waypoint会让Z下探到`-176.105mm`；新候选`[+/-90,27.3,-62.7]deg`可让第一段Y单调向内、Z从`-140`单调抬到约`-120mm`，两段峰值`|Y|=440mm`。
- Phase 31正式代码和回放已更新：抓取终点`Y=+/-440/Z=-140mm`，抓后抬高`19.993mm`且不下探，完整放置准备`max|Y|=440mm`；严格ARM GCC、全套回放和diff检查通过。
- Phase 31维护文档已同步并清理旧参数；正在补查共享A区profile对旧正式业务点的兼容性，避免AC专用Z约束扩大影响范围。
- Phase 31共享profile兼容性回放发现旧正式A点会因AC新waypoint下探62.483mm而失败；决定增加可选transfer waypoint/constraint字段，并只在AC单侧profile副本启用，公共A任务恢复原始直接过渡。
- Phase 31作用域修正完成：公共A左/A右profile的waypoint与约束开关保持0；AC单侧任务复制profile后才注入`[+/-90,27.3,-62.7]deg`、`|Y|<=440mm`和抬高`20+/-2mm`，旧正式A点不受影响。
- Phase 31最终验证完成：AC回放`waypointZ=-120.007mm/raiseZ=19.993mm/maxAbsY=440.000mm`，其余BD/AC/回正/后方路径、严格ARM GCC含HOST模式及`git diff --check`均通过；维护文档已明确AC副本边界，旧参数残留已清理。
- Phase 31完成。按本轮约束未启动Keil、未烧录、未进行机械臂实机运动验证，也未由本轮命令改写`Engineer.uvoptx`。
- Phase 32开始：保存已确认的D435i官方/Fusion外参但保持校准禁用；同时根据实机Watch截图诊断AC抓后不动。当前证据已定位到放置流程成功受理后的首段拒绝，而非主机故障或等待电机反馈。
- Phase 32完成：下位机已保存ID1轴心、D435i底部螺丝、RGB光心和117mm工具中心换算后的`t_E_C/R_E_C`，并以`CAMERA_TARGET_DEFAULT_CALIBRATED=0`保持不可执行；相机变换离线测试29项全部通过。
- 实机截图的AC抓后停止已定位为放置流程ACCEPTED后`SUBMIT_TRANSFER`首段预检拒绝；现有Watch可用`failure_source/fault`和`transfer_path_y/z_*`区分Y上限、Z抬高或ID1反馈问题。本轮未放宽440mm边界，也未修改放置轨迹。
- `gcc_arm_check.cmd`全部通过（含HOST模式），`git diff --check`无空白错误；未运行Keil、未烧录、未做新的机械臂实机动作验证。
- Phase 33完成：`APP_ARM_POSTURE_TEST_TRANSFER_PATH_Y_MAX_MM`由440mm改为445mm，只作用于AC单侧放置profile副本；抓取终点保持`+/-440mm`，名义轨迹峰值仍为440.000mm。
- AC/BD/回正/后方全套离线回放通过，输出确认`maxAbsY=440.000 limit=445.0`、抬高`19.993mm`；ARM GCC全部单元及HOST模式检查通过，`git diff --check`无空白错误。未运行Keil、未烧录、未做机械臂实机验证。
- Phase 34开始：用户再次多次观察到底座抽搐，现象与此前“到后方后又回抓取侧”相近。本阶段只读审计任务所有权、命令提交/完成条件、`+/-180deg`反馈边界和状态切换，不先改参数或轨迹。
- Phase 34初查：HOST模式下AC状态机只有`AppArmTask()`一个周期Poll所有者；USB任务只启动待处理请求，暂未发现两个任务同时提交q1。下一步定点核对`+/-180deg`后方终点、完成判定、反馈归一化和返回命令切换。
- Phase 34任务审计：Arm规划任务为AboveNormal/1ms，达妙发送任务为High/1kHz，职责为单规划写入、单电机发送；命令完成严格匹配active command ID。继续追踪`ArmUpdateJointReference -> ArmCommandPoseWithAxisSpeeds -> DMMotor`的q1数值连续性。
- Phase 34低层审计：q1到达妙目标为线性绝对位置，没有周期角连续化；若出现`+180/-180`符号切换会成为真实整圈目标跳变，但反馈链也未发现主动取模。已确认释放位姿和释放后抬臂会两次用瞬时q1反馈覆盖前一规划保持目标，是当前更强的抽动候选。
- Phase 34 resumed: re-read the live plan/findings/progress and dirty worktree. Confirmed the two post-rotation joint submissions use instantaneous q1 feedback despite the validated profile carrying an explicit rear q1 hold value. Continuing read-only verification of gripper-open completion and concrete rear-route values; no control code changed.
- Phase 34 completed as a source-level diagnosis. AC rear q1 is exactly `+/-180deg`; wrapped arrival checks accept up to `+/-180.2deg`, post-rotation commands copy the raw feedback, and joint interpolation wraps it to the opposite sign before the absolute Damiao command path. This explains intermittent near-360deg target discontinuities and repeated twitches. ID2 open completion and dual-task command ownership were excluded as primary causes. No arm/chassis/protocol source, build artifact, firmware image or hardware state was changed.
- Phase 35 started: user approved moving the rear targets to approximately `+/-178deg`. Selected the narrow fix of exact mirrored `+178/-178deg` profile targets plus explicit q1 hold in release-pose and release-clearance commands; global q1 trajectory semantics and all unrelated motion parameters remain out of scope.
- Phase 35 source patch applied: A LEFT/RIGHT rear targets are `+178/-178deg`; both post-rotation joint commands now explicitly hold the profile q1 instead of copying base feedback. No global trajectory, speed, AC Y/Z, tool, BD, chassis or protocol behavior was changed. Validation pending.
- Phase 35 validation pass 1 succeeded: full arm-path replay passed AC transfer (`maxAbsY=440.000/limit=445.0`, Z raise `19.993mm`), mirrored pick transitions, BD left/right and rear-route checks; strict Cortex-M4 GCC syntax checks passed all configured units and the explicit HOST-mode app runtime with warnings as errors. Updated four stale maintenance documents from `+/-180deg`/feedback hold to `+/-178deg`/explicit profile hold.
- Phase 35 residual documentation audit found and corrected three additional route summaries (`HANDOFF`, `FRUIT_TASK_FLOW`, `TUNING_GUIDE`). The global q1 soft-limit documentation remains correctly `[-180,180]`; only A/AC commanded rear placement is `+/-178deg`.
- Phase 35 complete. Final source and documentation residual searches passed, and `git diff --check` reported no whitespace errors beyond existing LF/CRLF notices. Keil build, flash and physical arm retest were not performed; the existing dirty `Engineer.uvoptx` was not touched by any build tool in this phase.
- Phase 36 started: recorded Phase 35 as the permanent AC open-loop fallback. Beginning a source-equivalent search for a left observation test point near tool center `[0,40,Z]mm`, base `q1=+90deg`, absolute tool pitch `-90deg`; communication remains deferred.
- Phase 36 resumed after context handoff. Recovered the live plan, findings, progress and dirty Git state; PowerShell again failed with `8009001d`, so all further inspection/validation uses native `cmd.exe`. The optional session-catchup helper again failed because MSYS Python cannot determine HOME; direct file recovery is complete.
- Phase 36 source audit confirmed the existing two-stage BD observation state machine and replay verifier are the correct paths to reuse. Preparing a temporary replay-only Z scan at fixed `[X,Y]=[0,+40]mm`, `q1=+90deg`, absolute pitch `-90deg`; no firmware behavior has been changed yet.
- Phase 36 candidate scan attempt 1: all target heights `180..320mm` failed with the legacy staging `[90,120,-48]deg`; failures occur on the staging-to-target line, not at the endpoint. Expanding the temporary offline search to staging q2/q3 while preserving all production safety limits.
- Phase 36 candidate scan attempt 2: no HOME-safe staging in the searched q2/q3 grid produced a continuous straight-line route to `Y=40mm/pitch=-90deg` at any tested Z. Next diagnostic separates endpoint IK/ID1/joint rejection from route topology.
- Phase 36 endpoint diagnosis complete: every fixed-Y40 endpoint needs ID1 below its `-90deg` lower limit (`-107.625..-130.401deg` across Z180..320). Proceeding with the smallest Y increase that preserves vertical pitch and practical ID1 margin; no production limit has been changed.
- Phase 36 tradeoff boundary: with vertical pitch and Y capped at 260mm, Z180..200 only touch the ID1 lower limit and Z>=210 is unusable. Extending the endpoint scan downward to locate a candidate with real ID1 margin before full-path validation.
- Phase 36 endpoint frontier found. Preparing full-path staging searches for rounded targets from `[0,100,100]mm` through `[0,190,150]mm`, all at `q1=+90deg/pitch=-90deg`; selection will favor smaller Y while retaining nontrivial safety margin.
- Phase 36 full-path frontier search passed all rounded targets, but revealed low staging trajectories under angle-only optimization. Tightening the temporary ranker to require about 8deg angular/ID1 margin and maximize whole staging-path minimum tool Z before selecting production constants.
- Phase 36 production configuration applied: default mode is BD observation test, active side LEFT, target `[0,+105,110]mm`, pitch `-90deg`, staging `[+90,90,-80]deg`; right point remains a strict mirror. Temporary scan code was removed and permanent replay passed both BD sides plus the preserved AC fallback. Core maintenance docs were updated; diff whitespace check passed.
- Phase 36 complete. Permanent replay passed BD LEFT/RIGHT (`final q=[+/-90,125.436,-44.186]deg`, staging `max|Y|=256.050/260mm`) and all preserved AC routes. Strict Cortex-M4 GCC checks passed every configured unit plus forced HOST app runtime with warnings as errors; `git diff --check` passed. No Keil build, flash, or physical arm motion was performed, and the user's dirty `Engineer.uvoptx` was not touched.
- Phase 37 started after the user corrected BD observation Y to about 400mm and explicitly confirmed AC/BD limits are independent. Planned source change is limited to mirrored BD Y=+/-400mm and BD replay limit=405mm; AC `+/-440mm` pickup and 445mm post-grip constraint remain untouched.
- Phase 37 source configuration applied and permanent replay passed: BD LEFT/RIGHT target Y is `+/-400mm`, final q is `[+/-90,56.100,-112.631]deg`, and BD `max|Y|=400/405mm`; preserved AC replay remains `max|Y|=440/445mm` with the existing 19.993mm post-grip Z raise.
- Phase 37 complete. Strict ARM GCC passed all configured sources and forced HOST runtime with warnings as errors; permanent replay simultaneously passed BD 400/405 and AC 440/445; maintenance docs and residual searches are synchronized, and `git diff --check` passed. No Keil build, firmware generation, flash, or physical motion test was performed; the existing dirty `Engineer.uvoptx` was not touched.
- Phase 38 started: raised the mirrored BD target Z from 110mm to 350mm. Direct pitch=-90deg failed at an intermediate ID1 lower-limit crossing; pitch=-60deg also failed near the endpoint. Neither attempt changed software limits or AC parameters.
- Phase 38 candidate selected: `[0,+/-400,350]mm`, base `q1=+/-90deg`, absolute pitch `-45deg`, staging `[+/-90,90,-80]deg`. Permanent replay passes both sides at final `q=[+/-90,69.663,-139.562]deg`; endpoint ID1 is about -74.225deg, retaining about 15.775deg lower-limit margin. Documentation synchronization and strict validation are in progress.
- Phase 38 complete. Permanent replay passed both BD mirrors (`max|Y|=400/405mm`) and the independent AC fallback (`440/445mm`); strict Cortex-M4 GCC checks passed all configured units plus forced HOST app runtime with `-Werror`, stale Phase 37 parameter searches were empty, and `git diff --check` passed. No Keil build, AXF/HEX generation, flash, or physical arm test was performed; the user's existing dirty `Engineer.uvoptx` was not touched.
- Phase 39 started and source point updated: BD LEFT is `[0,+200,350]mm`, RIGHT is its `[0,-200,350]mm` mirror, with pitch=-45deg and active side LEFT unchanged.
- Phase 39 permanent replay passed both mirrors at final `q=[+/-90,114.050,-96.794]deg`; endpoint ID1 is about -75.844deg with 14.156deg lower-limit margin. Whole-path `max|Y|=338.781/405mm` because staging extends farther than the endpoint; AC remains `440/445mm`. Strict validation and final documentation audit are pending.
- Phase 39 complete. Strict Cortex-M4 GCC passed all configured units and forced HOST app runtime with `-Werror`; permanent replay passed BD LEFT/RIGHT and the independent AC fallback, current-source/document searches contain only `[0,+/-200,350]mm`, and `git diff --check` passed. No Keil build, AXF/HEX generation, flash, or physical arm test was performed; the user's existing dirty `Engineer.uvoptx` was not touched.
- Phase 40 started and preferred pitch=-30deg applied at the unchanged `[0,+/-200,350]mm` BD targets. Permanent replay passed both mirrors at final `q=[+/-90,120.249,-87.711]deg`; endpoint ID1 is about -57.960deg with 32.040deg lower-limit margin, so the -35deg fallback is unnecessary. Replay now also reports whole-path `minZ=102.373mm` and `max|Y|=357.375/405mm`.
- Phase 40 complete. Strict Cortex-M4 GCC passed all configured units and forced HOST app runtime with `-Werror`; permanent replay passed BD LEFT/RIGHT and independent AC fallback, BD maintenance documents reflect pitch=-30deg, and `git diff --check` passed. No Keil build, AXF/HEX generation, flash, or physical arm test was performed; the user's existing dirty `Engineer.uvoptx` was not touched.
- Phase 41 corrected the pitch direction after user clarification. Full replay rejected -75/-70/-65/-60deg on the ID1 lower limit; -55deg passed with only 2.731deg endpoint margin, and -50deg passed with 8.395deg margin. The user selected final pitch=-53deg.
- Phase 41 source and maintenance docs now use `[0,+/-200,350]mm/pitch=-53deg`. Both mirrors pass at final `q=[+/-90,110.456,-101.558]deg`; endpoint ID1 is about -85.014deg with 4.986deg lower-limit margin, whole-path `minZ=80.112mm`, and `max|Y|=326.462/405mm`. Strict validation is pending.
- Phase 41 complete. Strict Cortex-M4 GCC passed all configured units and forced HOST app runtime with `-Werror`; permanent replay passed both BD mirrors and the independent AC fallback, current maintenance docs contain pitch=-53deg with no stale current -30deg value, and `git diff --check` passed. No Keil build, AXF/HEX generation, flash, or physical arm test was performed; the user's existing dirty `Engineer.uvoptx` was not touched.
- Phase 42 complete: marked both confirmed BD observation poses as the fixed baseline for the upcoming upper/lower-controller protocol update. Source and handoff comments now require protocol code to select LEFT/RIGHT without duplicating geometry; no motion value, wire format or runtime behavior changed. Strict ARM GCC including forced HOST mode and `git diff --check` passed; no Keil build, flash or physical motion was performed.
- Phase 43 started: audited the user-replaced generated protocol trio. Wire hash/layout remain `0x740E426B` and already match Phase 29; maintained runtime/USB queueing must be preserved. The semantic gap is task 4 QR pose and task 5 current area. Implementing area state/callback now; QR pose will receive an explicit unsupported Watch path because no motion target exists in source.
- Phase 43 bridge patch applied: task 5 stores typed A/B/C/D state independently of active actuator commands, exposes `UpperControllerGetCurrentArea()`, and defers callback 5 executing/completed to the next bridge task after the generated ACK path. Task 4 is explicitly recognized but only increments request/unsupported counters; it cannot move or complete until a QR pose is defined. Generated files, protocol runtime/port, confirmed BD geometry and AC behavior were not changed by the bridge patch.
- Phase 43 complete. Strict Cortex-M4 GCC passed generated protocol, maintained runtime/port/bridge and forced HOST app runtime with `-Werror`; permanent replay retained both BD mirrors (`minZ=80.112mm`, `max|Y|=326.462/405mm`) and AC (`440/445mm`), and `git diff --check` passed. No Keil build, AXF/HEX generation, flash, live USB exchange or QR pose motion was performed; the user's existing dirty `Engineer.uvoptx` was not touched.
- Phase 44 started: user requested AC closed-loop bring-up. New HOST behavior is `StateMachineCommand task_id=2/status 0 or 1 -> left/right observation pose`, then `ArmTarget -> camera-to-base transform -> fixed Z=-100mm pick`. Generated protocol files remain unchanged; implementation is limited to maintained bridge/runtime/flow code and documentation.
- Phase 44 source patch applied: `task_id=2` no longer starts AC open-loop complete pick/place in HOST mode; it moves to the confirmed mirrored observation point, captures a pose snapshot, and sends callback 2 executing/completed. `ArmTarget` now requires observation holding, fresh snapshot, successful transform and idle arm, then calls `AppArmFlowStartPick()` with `X/Y` from the transform and fixed `Z=-100mm`; successful start consumes the observation state, and callback 3 reports closed-loop pick executing/completed.
- Phase 44 complete. Docs synchronized across README, HANDOFF, FRUIT_TASK_FLOW, CAMERA_TARGET_TRANSFORM, TUNING_GUIDE, PROJECT_OVERVIEW and planning records. AC open-loop side pick/place remains preserved as a fallback module. Strict Cortex-M4 GCC, permanent arm-path replay, camera transform 29 checks and `git diff --check` all passed. No Keil build, AXF/HEX generation, flash, live USB exchange or physical arm test was performed; the user's existing dirty `Engineer.uvoptx` was not touched by any build tool in this phase.
- Phase 45 complete: default `APP_MODE` changed from `APP_MODE_ARM_BD_OBSERVATION_TEST` to `APP_MODE_HOST_CONTROL`, with surrounding comments and current-mode docs synchronized. BD observation test remains available as a manual mode; no motion geometry, protocol layout, extrinsic, speed or path parameter changed.
- Phase 46 complete: AC/BD closed-loop observation target changed from `[0,+/-200,350]mm` to `[0,+/-150,300]mm`, keeping pitch `-53deg`, staging `[+/-90,90,-80]deg`, speed `150mm/s` and BD replay limit `405mm`. Permanent arm-path replay, strict Cortex-M4 GCC including forced HOST runtime, current-doc residual search and `git diff --check` all passed. No Keil build, AXF/HEX generation, flash or physical arm test was performed.
- Phase 47 started: user requested moving the current AC/BD observation pitch 5deg closer to vertical downward. Applied `APP_ARM_BD_OBSERVATION_TOOL_PITCH_DEG=-58deg` at the unchanged `[0,+/-150,300]mm` targets; staging remains `[+/-90,90,-80]deg`, speed remains `150mm/s`, and the BD-only replay limit remains `405mm`.
- Phase 47 complete. Permanent replay passed both BD mirrors at final `q=[+/-90,123.291,-84.168]deg`; staging ID1 relative pitch is `-48deg`, endpoint ID1 relative pitch is about `-85.459deg`, whole-path `minZ=77.557mm` and `max|Y|=318.051/405mm`. Strict Cortex-M4 GCC including forced HOST runtime and `git diff --check` passed. No Keil build, AXF/HEX generation, flash or physical arm test was performed.
- Phase 48 started: user requested AC closed-loop ArmTarget picking to keep the gripper vertical downward in addition to the already fixed Z. Changed `APP_ARM_AC_CLOSED_LOOP_PICK_TOOL_PITCH_DEG` from the AC open-loop `-5deg` alias to a dedicated `-90deg`; ArmTarget still uses transformed base-frame X/Y and fixed `Z=-100mm`.
- Phase 48 complete. Strict Cortex-M4 GCC including forced HOST runtime passed; permanent path replay still passes BD observation and AC open-loop fallback paths; `git diff --check` passed with only existing LF/CRLF warnings. No Keil build, AXF/HEX generation, flash, live USB exchange or physical arm test was performed.
- Phase 49 resumed from the implementation handoff. Re-read the embedded routing and file-planning instructions, recovered the live planning records and confirmed the dirty worktree contains the expected shared-planner changes without touching `Engineer.uvoptx`.
- Phase 49 application integration was simplified: `AppArmFlowSelectReachablePickAdvance()` now delegates the complete `staging -> approach -> advance/fallback` decision to `ArmTrajectorySelectReachableToolCenterAdvance()` and only performs input validation, staging construction, target update and diagnostic mapping. The duplicate application-layer segment planning and fallback arithmetic were removed.
- Phase 49 validation pass after the orchestration change succeeded. `Engineer/MDK-ARM/gcc_arm_check.cmd` passed every configured unit and the forced HOST_CONTROL runtime with `-Werror`; `tools/arm_path_replay/run.cmd` retained the existing BD, AC transfer, mirrored pick and front/rear route outputs. Direct advance-selection and planner-boundary tests are still pending.
- Phase 49 documentation log error: the first `findings.md` patch used a non-matching Phase 49 heading and was atomically rejected. Re-read the actual heading and appended through the stable final bullet of that section; no source file was affected by the failed attempt.
- Phase 49 boundary-vector scan attempt 1 failed before compilation because direct Windows GCC tried to create a temporary file under `C:\WINDOWS` and received permission denied. Switched to a dedicated temporary CMD wrapper that sets `TMP`/`TEMP` to the replay `_tmp` directory, matching the established HOST replay environment workaround.
- Phase 49 direct production-planner tests were added to `tools/arm_path_replay/arm_path_replay.c`. They pass mirrored 30mm full advance, a concrete 29mm fallback, exact 1mm acceptance, 0mm rejection, approach-failure diagnostics, repeated-preflight consistency, normal/AC q1 limits, q2/q3 endpoints, ID1 endpoints, the 1536-sample capacity boundary and NaN/Inf rejection. The temporary boundary scanner source/script/executable were removed after extracting stable vectors.
- Phase 49 explicit-policy pass attempt 1 changed the legacy `ArmMoveLinearToolCenter()` signature and strict GCC rejected it because `arm_internal.h` exposes the existing two-argument compatibility declaration. HOST replay still passed. Resolution is to preserve the public internal wrapper and add a file-local policy-explicit implementation used by command submission.
- Phase 49 explicit-policy pass completed: production command submission now passes `command->safety_profile` through a file-local tool-center move implementation into every shared segment request; the legacy two-argument internal wrapper remains compatible and explicitly uses NORMAL. Firmware preflight wrappers inject the existing ID1 service hook when callers omit one. Strict ARM GCC and the expanded HOST replay both pass after this correction.
- Phase 49 Keil full-rebuild attempt 1 used project-local TEMP/TMP with `UV4.exe -r -j1`, but after more than two minutes it had created neither the requested log nor fresh Objects/Listings artifacts. The single CLI session was interrupted to avoid an idle background process. This is the repository's previously recorded UV4 non-start behavior; no Keil pass is claimed. A second attempt will use the previously successful `-cr -j0` invocation shape rather than repeating this command.
- Phase 49 Keil attempt 2 with the established `-cr -j0` flow built successfully but exposed one ArmCC-only unused-function warning for the disabled front-barrier helper. Wrapped that helper in the same feature condition as its two call sites; strict GCC and HOST replay remained green.
- Phase 49 final diagnostics preserve the original 30mm limiting failure alongside the successful truncated plan and explicitly distinguish staging-to-approach failure from advance IK/joint-limit failure. Expanded HOST assertions pass with the same 30/29/1/0mm selections.
- Phase 49 complete. Strict Cortex-M4 GCC including forced HOST_CONTROL passed with `-Werror`; HOST replay passed legacy BD/AC/front-rear routes plus direct shared-planner boundary and consistency tests; Keil ArmCC 5.06u7 full rebuild produced fresh AXF/HEX/MAP with 0 errors and 0 warnings and MAP-linked shared planner symbols. `git diff --check` passed with only line-ending notices, and `Engineer.uvoptx` remained byte-identical. Firmware was not flashed and no physical arm/camera test was performed.
- Phase 49 verification-tool error: a multi-pattern `findstr` command was parsed incorrectly by native cmd while filtering the Keil log. Directly reading the 3KB log and using `rg` on the MAP provided the required evidence; the failed filter did not modify files or affect the build.
- Phase 49 final residual-search note: two quoted `rg` expressions containing spaces/alternation were split by native cmd and produced noisy false path errors. Replaced them with single-token searches for the q2 documentation line, completed phase marker and Keil project entry; no source or artifact was affected.
- Phase 50 started: user requires the upper-controller HOME/reset command to execute again so later workflow is not blocked, while preserving enough latched rejection diagnostics to inspect the failure after reset. Scope is the maintained protocol bridge/reset state machine and Watch diagnostics; wire format and motion safety checks remain unchanged.
- Phase 50 implementation removed the temporary post-ArmTarget-failure HOME block. Every valid `task_id=6,status=0` now enters the existing cancel-then-HOME state machine and retains its existing callback 6 executing/completed semantics.
- Added `g_upper_arm_reject_diagnostic` as an independent last-failure Watch snapshot. ArmTarget and reset-HOME failures capture their source, stage, command/fault chain, IK/workspace/preflight coordinates and AC advance diagnostics before live state cleanup; reset does not erase the snapshot.
- Phase 50 initial verification passed strict Cortex-M4 GCC with `-Werror` and the full HOST path replay, including shared advance and planner-boundary tests. Keil full rebuild and final diff checks are pending.
- Phase 50 complete. Keil ArmCC 5.06u7 full rebuild generated fresh AXF/HEX/MAP with 0 errors and 0 warnings. MAP places the 132-byte rejection snapshot in normal `.bss`; final size is Code=143232, RO-data=3648, RW-data=1432, ZI-data=137560. `git diff --check` passed with line-ending notices only, and `Engineer.uvoptx` remained byte-identical at SHA-256 `6ae31918e053173831440f0220423ff8d652ee822188d32c34f9ddc02a018fcb`. Firmware was not flashed, no live upper-controller exchange was performed, and the physical arm was not moved.
- Phase 52 started: recovered the existing Phase 49-51 planning context and classified the current issue as application-layer Cartesian path planning. The exact right-side target fails on staging-to-approach sample 341 before advance fallback; endpoint reachability is being verified separately before changing the route.
- Phase 52 source audit: the shared advance request currently supports only one straight staging-to-approach segment. Preparing a production-planner HOST regression for the exact field coordinate and a bounded waypoint search; no limits or source behavior have been changed yet.
- Phase 52 exact HOST reproduction: production IK returned `ARM_IK_OUT_OF_REACH` for both the field approach and nominal 30mm final endpoint. A waypoint-only route search correctly found no solution because the endpoint itself is outside geometric reach. Firmware behavior remains unchanged.
- Phase 52 diagnostic cleanup: removed the temporary field scanner and reran the complete HOST replay successfully, including shared 30/29/1/0mm advance cases and planner boundaries. `git diff --check` reports line-ending notices only. Awaiting a business decision because making this target executable requires coordinate correction, chassis repositioning, or a target clamp well beyond the previously accepted 30mm tolerance.
- Phase 53 started: field evidence isolated the stopped workflow to an online ID2 close timeout, not arm motion or place preflight. Planned behavior is to convert only the bounded close-timeout relief outcome to `FORCED_HELD`, while preserving hard faults, and change AC closed-loop pick pitch from -15deg to -20deg.
- Phase 53 implementation pass 1: timeout relief now targets/completes as `FORCED_HELD` only with valid ID2 feedback, retaining the timeout error for Watch; hard tool failures remain latched. AC closed-loop pitch is -20deg. Initial replay invalidated the old -15deg 29mm boundary because it now accepts 30mm, so boundary vectors are being regenerated. The first GCC invocation used the wrong working directory and did not compile source.
- Phase 53 boundary scan complete: at X=-160mm and pitch=-20deg, the stable shared-planner vectors are 30mm through Y=545, 29mm at 546, 1mm at 574 and 0mm at 575; approach fails from 576. This is about 3mm farther than -15deg. Strict ARM GCC then passed from the correct MDK-ARM working directory.
- Phase 53 near scan pass 1: at X=0 the -20deg production planner rejects approach through Y=240mm and accepts a full 30mm advance from Y=245mm. A 1mm boundary scan is pending. Replay also exposed one stale repeated-preflight input (543 instead of the new 546 fallback vector), which will be corrected with the permanent test update.
- Phase 53 edit note: the first combined near-boundary/test patch was atomically rejected because of an invalid multi-file hunk separator; no source was partially changed. The edit was split into smaller patches.
- Phase 53 documentation note: the first combined documentation patch was atomically rejected because mojibake in the displayed context did not match the UTF-8 file. Stable numeric lines were patched separately, then the gripper paragraph was located by the ASCII `ARM_GRIPPER_FORCED_HELD` token and updated successfully.
- Phase 53 production validation pass: full shared-planner replay passed new -20deg 30/29/1/0mm vectors, exact field 30mm, and 245-pass/244-reject near boundaries. Strict ARM GCC, all 29 camera-transform checks and diff whitespace checks passed. Keil full rebuild is pending.
- Phase 53 final parameter override: user restored AC closed-loop pitch to -15deg and lowered fixed Z from -105mm to -115mm. Production-planner scan moved the X=0 near boundary to 275mm (274mm rejects), so the 30mm near clamp now accepts 245..275mm. At X=-160mm the permanent far vectors are 536mm/full30, 537mm/29, 565mm/1, 566mm/0 and 567mm/approach failure. Full HOST replay passed after removing the temporary scan loops; ARM GCC and Keil rebuild are pending for the final constants.
- Phase 53 final Keil attempt 1 did not create a target because ArmCC could not create temporary files. The misleading summary still printed 0 errors, but the log explicitly says `Target not created`; this attempt is not accepted. `Engineer.uvoptx` remained byte-identical. Retry will use an explicit project-local TEMP/TMP directory.
- Phase 53 complete. ID2 online/valid close timeout now degrades through bounded relief to `FORCED_HELD`, clears the active gripper fault latch, preserves `SERVO_TIMEOUT` diagnostics even if all relief attempts expire, and lets the normal post-grip dwell/place flow continue; offline, TX, initialization, boot-relief and nonrecoverable faults still stop. Final AC parameters are `Z=-115mm/pitch=-15deg`, near clamp `245..275 -> 275mm`, and 30mm advance with positive-prefix fallback. Full HOST replay, strict ARM GCC, all 29 camera-transform checks and final Keil ArmCC rebuild passed. Keil final size is Code=144000, RO-data=3660, RW-data=1432, ZI-data=137560 with 0 errors/0 warnings; `Engineer.uvoptx` SHA-256 remained `2637742439bdf9687c6431fa1ebd627a3a95668b9cdcfdb82f0cc17f74045f79`. Firmware was not flashed and no physical-arm validation was performed.
- Phase 54 started: AC post-grip placement currently changes ID1 from the start of a single waypoint-to-release route. The implementation will split this at the existing transfer waypoint: first reach `[side q1=+/-90,q2=75,q3=-62.7]deg` with the current ID1 relative angle locked and require at least 50mm actual tool-center Z rise; only after command completion may the existing rear rotation/release-pitch command start. Non-AC profiles remain unchanged.
- Phase 54 edit attempt 1 was atomically rejected because the expected TUNING_GUIDE table text did not match the current document; no source or documentation file was partially changed. The source/config patch and documentation patch will be applied separately using current text.
- Phase 54 implementation split the constrained AC placement route into two commands. The first command targets only `[side q1=+/-90,q2=75,q3=-62.7]deg` with `tool_relative_pitch_valid=0`, waits in `WAIT_TRANSFER`, and uses a strict runtime FK threshold of at least 50mm Z rise with zero shortfall tolerance. Only after completion does the existing directed release rotation set the release ID1 pitch. HOST replay reports ID1 locked through the waypoint, 331.370mm nominal Z rise and 449.671/455mm Y usage; strict ARM GCC also passed.
- Phase 54 complete. Keil ArmCC 5.06u7 full rebuild generated AXF/HEX with Code=143972, RO-data=3660, RW-data=1432, ZI-data=137560 and 0 errors/0 warnings. `Engineer.uvoptx` SHA-256 remained `7f7f25bcb6c3af130f5ee6df4c7775660d1905de5c0eac213253bf4f6606c29d` across the build. Firmware was not flashed and the 50mm/ID1 sequencing has not yet been physically validated.
- Phase 55 started: user wants the post-grip transfer waypoint to remain continuous and also wants a raised waypoint between observation and target approach. Auditing the existing continuous joint staging and scanning production FK/path constraints before selecting geometry; protocol and global limits remain unchanged.
- Phase 55 implementation pass: AC constrained placement now uses one continuous joint command through the q2=75deg transfer waypoint to rear release q with ID1 locked, followed by a same-q ID1 release-pitch command. Pick staging is q2=110deg/q3=-110deg and still uses the existing no-stop base-aim waypoint command.
- Phase 55 HOST pass 1: continuous post-grip geometry passes both mirrors (`raiseZ=331.370mm`, `max|Y|=449.671/455mm`), but shared advance regression stops at stale `275/274mm` near vectors after staging changed. A temporary production-planner boundary scan is being run and will be removed afterward.
- Phase 55 staging comparison: q2=110/q3=-110 raised the near boundary to286mm; q2=100/q3=-110 still raises the physical tool center about58mm but restores all existing near/far advance vectors. Selected the latter, removed the temporary scan, and added a permanent mirrored observation-to-high-staging safety/height regression.
- Phase 55 environment/errors: managed PowerShell failed to load with Windows error `8009001d`, so checks use native `cmd.exe`. Quoted `rg` expressions containing spaces/alternation were split by cmd and replaced with single-token searches. One combined planning/scan patch was atomically rejected on unmatched findings context and then split using current file text. None of these errors partially modified source.
- Phase 55 final HOST/ARM validation: `tools/arm_path_replay/run.cmd` passes AC post-grip continuous transfer, mirrored observation-to-high staging (`centerZ=358.423mm`, `raiseZ=58.423mm`), post-place transitions, AC 30/29/1/0mm fallback vectors, field vector and planner boundary tests. `Engineer/MDK-ARM/gcc_arm_check.cmd` passes all sources and forced HOST_CONTROL with `-Werror`; `git diff --check` reports no whitespace errors. Final staging is `[q1,100,-110]deg`, and the old near/far vectors remain valid.
- Phase 55 build gap: no `UV4.exe` was found in PATH, common Keil locations or registry, so Keil full rebuild and firmware generation were not run. `Engineer.uvoptx` hash remains `7f7f25bcb6c3af130f5ee6df4c7775660d1905de5c0eac213253bf4f6606c29d`; firmware was not flashed and no physical-arm validation occurred.
- Phase 56 correction: the first `-15deg` sign made the right camera physically higher, proving this servo's positive output direction is downward. Changed `MG995_SERVO_RIGHT_MECHANICAL_OFFSET_DEG` to `+15deg`; logical 90deg now writes the calibrated physical 105deg pulse (`about 1583us`). Camera angle conversion and debug logical angles remain centered at 90deg.
- Phase 57 started: user要求B区观察后接收ArmTarget并执行抓取。当前门控只允许AC，且目标构造无条件套用AC固定Z、近端Y钳位、30mm推进和-15deg俯仰；本阶段将新增B区水平抓取分支，使用解算XYZ和AC同样的左右X补偿，抓取完成后暂不复用未定义的BD放置profile。
- Phase 57 complete: `APP_ARM_BD_CLOSED_LOOP_PICK_TOOL_PITCH_DEG=0deg`; B区ArmTarget门控已开放，目标使用变换后的基座系Y/Z、AC相同的左右X补偿，不做AC近端钳位、固定Z或30mm推进。B区沿用统一抓取状态机，抓取完成后保持闭爪并callback 3 completed，不调用AC放置profile。ARM GCC `-Werror`、HOST回放和`git diff --check`通过；未烧录、未做实机动作验证。
- Phase 58 complete: `UpperControllerApplyCurrentArea()` now sets both MG995 camera angles to `-45deg` for AC group (A/C) and `+45deg` for BD group (B/D) immediately after the current-area command; `task_id=2` side selection and observation motion are unchanged.
- Phase 59 complete: normal joint and Cartesian speed caps are now q1/q2/q3=`180deg/s` (`ARM_JOINT_Q*_COMMAND_SPEED_DEG_S` and `ARM_LINEAR_Q*_MAX_SPEED_DEG_S`); all q2/q3 acceleration limits remain `800deg/s2`.
