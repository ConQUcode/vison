# Task Plan: Y轴镜像、480抓放测试、上电初始化与释放掉线恢复

## Goal

当前世界坐标统一为 `+Y` 物理左侧、`-Y` 物理右侧；临时定姿测试终点为 `[0,480,-150] mm`、绝对俯仰`-5deg`，到位后完成一次抓取和物理左侧放置。上电初始化、ID2释放短时掉线恢复以及用户确认的q2和ID1软件边界保持有效。

## Phases

1. [complete] 审计当前启动状态机、耦合补偿、工具初始化和抓放接口。
2. [complete] 设计并实现 `[0,-480,-135]` 到位后的闭合与左侧放置流程。
3. [complete] 实现大臂优先的上电初始化顺序和故障/Watch状态。
4. [complete] 同步配置、Watch和文档。
5. [complete] 执行静态路径核对、符号搜索和 `git diff --check`，不编译、不烧录。
6. [complete] 建立应用层统一机械臂命令ID分配器，迁移专项测试和抓放流程，接入Keil工程并完成文档同步与静态验证。
7. [complete] 完成侧向测试俯仰全路径预检（-5deg因ID1限位不可用，保留-10deg），并把上电HOME顺序改为大臂/小臂同步、随后底座、最后ID1/ID2；完成文档同步和Keil验证。
8. [complete] 将侧向专项测试绝对俯仰由-10deg调整为已通过全路径预检的-7deg，并同步当前模式文档；初始化顺序保持q2+q3同步、随后q1和ID1/ID2。
9. [complete] 为ID2释放张开增加有界的短时反馈掉线恢复，持续离线或动作超时仍锁存失败，并补充可区分掉线/恢复/超时的Watch。
10. [complete] 复算当前抓取路径继续降低Z时的首个拒绝约束；确认首先受q2软下限限制后，将q2普通运动下限由10deg调整为3deg并完成全路径静态校验。
11. [complete] 按用户确认将ID1最小控制值放宽到115，同步相对角上限、自检和当前`-5deg/-150mm`专项路径，并完成全路径及语法验证。
12. [complete] 整体镜像机械臂世界Y轴，使`+Y`为物理左侧；同步点位、FK/IK、工具偏移、应用预对准、后方绕行、离线回放工具和文档，并验证物理关节目标保持不变。
13. [complete] 将定姿专项测试扩展为左右镜像持续交替抓放：左侧`[0,340,-150] -> [0,480,-150]`，右侧只对Y取负；分别使用A左/A右放置profile，并验证两侧IK、关节限位、ID1范围、工作空间路径和故障锁存完全对称。
14. [complete] 诊断实机右侧循环在底座预对准后不再运动的问题：真实左放置回正姿态到右侧接近点的首个直线采样触发前方栏框大臂限制，命令在DM大臂/小臂实际运动前被预检拒绝。
15. [complete] 长期修复专项测试的左右循环边界：每侧底座预对准时同步提交q1、抓取准备q2/q3和ID1相对俯仰，再执行镜像工具中心轨迹；安全限位和抓取坐标保持不变，真实释放姿态过渡回放与严格语法检查均通过。
16. [complete] 将单侧完整抓取和对应侧释放从`app_runtime.c`整理为可复用的非阻塞任务接口：一个带LEFT/RIGHT参数的Start函数、周期Poll和统一状态；当前默认模式继续通过该接口自动左/右交替，USB协议本轮不改。
17. [complete] 配置TIM8_CH2/CH3为PI6右MG995、PI7左MG995，新增独立90度双舵机测试模式；该模式不初始化或运行机械臂、底盘和IMU，并保留现有抓放联调代码供后续切回。
18. [complete] 新增底盘连续`vx/wz`内部命令接口：支持运行中刷新、命令超时平滑停车、联合轮速比例限幅，以及`wz=0`直行时的IMU航向保持；本轮不新增上位机协议包，当前MG995测试模式保持不变。
19. [complete] 同步新版上位机三文件：适配可选握手/非严格心跳，新增独立上位机控制模式和业务桥，接通底盘速度及夹爪/摄像头命令；ArmTarget仅校验与观察，等待相机外参方案后再执行。
20. [complete] 实现末端摄像头目标坐标转换：建立拍照姿态快照、可配置且强校验的相机外参、摄像头系到机械臂基座系的完整刚体变换和离线数学测试；外参未标定或快照未关联时ArmTarget继续禁止执行。
21. [complete] 将BD左侧观察点保存为工具中心`[0,57,210] mm`、世界绝对俯仰`-30deg`，保持底座朝左`q1=+90deg`与当前打点模式；完整路径、严格检查和Keil打点固件全量重建均通过。
22. [complete] 将默认固件切换为`APP_MODE_ARM_BD_OBSERVATION_TEST`，保留阶段21已验证的左侧观察点参数；严格检查和Keil全量重建均通过。
23. [complete] 修正BD观察点第一段障碍路径：底座旋转时同步调整臂关节，使实际夹爪中心轨迹Y不超过260mm，再从安全过渡姿态直线进入`[0,57,210]mm`；完成两段全路径验证和Keil重建。
24. [complete] 将默认BD观察测试从左侧完整镜像到右侧：底座目标改为`q1=-90deg`、目标Y改为`-57mm`，保持q2/q3/俯仰/速度不变；验证两段`|Y|<=260mm`并完成Keil重建。
25. [complete] 将BD左右观察点改为显式双点配置并加清晰注释，通过单一侧别宏选择当前执行点；当前保持RIGHT，并让离线回放每次同时验证左右两点后重建上传固件。
26. [complete] 将默认固件切换为`APP_MODE_HOST_CONTROL`，确认上位机`VelocityCommand`经USB协议桥进入连续底盘速度接口，保留300ms超时平滑停车与现有限幅，并完成host模式Keil重建和MAP调用链核对。
27. [complete] 将上位机底盘线速度上限提高到`1.0m/s`，同步提高角速度、单轮速度和加速度边界；把有限超限`VelocityCommand`从整包INVALID改为逐字段饱和钳位，并完成严格检查、Keil重建和文档同步。
28. [complete] 接入新增`StateMachineCommand task_id=2`的AC区抓取方向控制：status 0左、1右、2左右依次执行，使用callback_id 2报告开始/完成，并在HOST模式启用现有单侧非阻塞抓放任务。
29. [complete] 同步上位机新协议`PROTOCOL_HASH 0x740E426B`：清理已删除的`FruitDetection`维护层和工程成员，保留AC区`callback_id=2`语义，将`ArmTarget callback_id=3`作为真实执行预留，并完成严格检查、Keil重建与MAP核对。
30. [complete] 限制AC区抓取完成到放置准备的夹爪中心轨迹：新增左右镜像的受约束过渡步骤和运行时实际反馈预检，逐采样保证`|Y|<=430mm`，保留现有全局安全保护和后方放置逻辑，并扩展离线回放验证。
31. [complete] 调整AC抓取点和抓后收拢高度：左右抓取终点改为`Y=+/-440mm`，接近/抓取Z统一提高到`-140mm`；重新求解左右镜像waypoint，使抓后向内收时工具中心不先下探并抬高约20mm到`Z=-120mm`，完整放置准备保持`|Y|<=440mm`，同步运行时预检、回放和文档。
32. [complete] 保存已确认的D435i官方/Fusion外参到下位机禁用配置；诊断实机AC抓后停在放置首步的问题，确认拒绝来源并复用现有Watch精确区分Y/Z预检，不放宽轨迹安全边界。
33. [complete] 将AC抓后收拢轨迹运行时Y峰值上限由440mm调整为445mm，保持抓取终点、waypoint和全局工作空间限制不变，并完成回放与语法检查。
34. [complete] 排查AC抓放过程中底座间歇性抽搐：确认后方`q1=+/-180deg`到位容差、瞬时反馈重锁定与轨迹`WrapTo180`共同造成跨边界绝对目标跳变；夹爪完成条件和任务唯一所有权均已排除为主因，本阶段只诊断、未修改运动逻辑。
35. [complete] 修复AC/A区后方底座抽搐：按用户确认将左右后方目标内收到`q1=+/-178deg`，释放位姿和释放后抬臂显式保持profile q1；左右镜像、全路径回放、ARM严格编译、文档残留与差异检查均已通过。
36. [complete] 建立闭环方案左侧观察姿态测试：保留阶段35为AC开环备选；确认`Y=40mm`在竖直向下时超出ID1范围后，选定工具中心`[0,+105,110]mm`、底座`q1=+90deg`、绝对俯仰`-90deg`及staging`[+90,90,-80]deg`，验证HOME完整路径并切换默认测试模式。
37. [complete] 按用户纠正将BD闭环观察点Y改为左右`+/-400mm`，BD专用离线Y验证上限改为405mm；保持Z=110mm、绝对俯仰-90deg和现有staging，左右完整路径通过，同时确认AC抓取`+/-440mm`及445mm抓后约束完全不变。
38. [complete] 将BD闭环观察点高度提高到350mm；确认Y=+/-400mm可保留，将竖直向下俯仰调整为斜向对应侧的-45deg，双侧完整路径、AC永久回放和严格编译均通过。
39. [complete] 将BD左观察点Y由+400mm改为+200mm，右侧继续由左侧严格镜像为-200mm；保持Z=350mm、俯仰-45deg和AC参数不变，双侧完整路径及严格编译均通过。
40. [complete] 将BD世界绝对俯仰由-45deg改为优先值-30deg；双侧完整路径直接通过，无需回退为-35deg，保持`[0,+/-200,350]mm`和AC参数不变。
41. [complete] 根据用户澄清重新向竖直向下方向选择BD俯仰：-75/-70/-65/-60deg均失败，-55deg余量偏小；按用户最终选择采用-53deg并通过双侧完整路径及严格编译。
42. [complete] 将已确认的BD左右观察姿态标记为后续上下位机通信接入基准；明确协议层只选择LEFT/RIGHT，不复制或覆盖运动参数，并更新下一步协议同步交接说明。
43. [complete] 审计用户替换的生成协议并同步业务桥：保持`0x740E426B`线格式和既有传输运行层，接入`task_id=5`当前区域及callback 5；对缺少点位定义的`task_id=4`二维码姿态只做显式未配置诊断，不伪报执行完成。
44. [complete] 接入AC闭环抓取第一版：HOST下`task_id=2/status 0/1`只触发左/右观察姿态并保存pose snapshot，`status 2`判为无效；随后`ArmTarget`使用最新观察快照和已启用D435i外参换算基座坐标，抓取`X/Y`取换算值、`Z=-100mm`，通过`AppArmFlowStartPick()`执行到点闭合并消费观察状态，用callback 3报告执行中/完成。
45. [complete] 退出默认BD观察点测试模式，切回`APP_MODE_HOST_CONTROL`，使上电默认进入上位机协议控制；BD观察测试入口保留为手动切回模式，运动参数不变。
46. [complete] 按用户要求调整AC/BD左右闭环观察点：Z从350mm降到300mm，Y向零点靠50mm，最终为`[0,+/-150,300]mm`；保持俯仰-53deg、staging、速度和BD 405mm回放上限不变，并通过路径回放。
47. [complete] 按用户要求将AC/BD观察姿态再向竖直向下靠5deg：世界绝对俯仰由-53deg改为-58deg，保持`[0,+/-150,300]mm`、staging、速度和BD 405mm回放上限不变；双侧路径回放、ARM严格GCC和差异空白检查通过。
48. [complete] 将AC闭环ArmTarget抓取姿态补充为夹爪竖直向下：换算后仍只取基座系X/Y，Z固定`-100mm`，抓取世界绝对俯仰由复用AC开环`-5deg`改为闭环专用`-90deg`；AC开环备选参数不变，ARM严格GCC、路径回放和差异空白检查通过。

## Locked Decisions

- 当前默认应用模式为`APP_MODE_HOST_CONTROL`，上电初始化USB、底盘、IMU、机械臂、ID2和双MG995并响应上位机协议；`APP_MODE_ARM_BD_OBSERVATION_TEST`只作为手动切回的左侧观察点测试入口保留。
- 当前AC/BD闭环观察点为`[0,+/-150,300]mm/pitch=-58deg`，右侧只由左侧Y/q1严格镜像生成；协议层只能选择LEFT/RIGHT，不复制或覆盖该几何参数。
- 阶段44覆盖阶段28/29的HOST `task_id=2`运行语义：当前`task_id=2/status 0`进入AC左观察，`status 1`进入AC右观察，`status 2`无效；不再由HOST命令直接执行AC开环完整抓放或双侧串行抓放。
- 当前`ArmTarget`允许在AC观察完成保持后触发闭环抓取：使用最近一次观察pose snapshot，最大年龄5000ms；`X/Y`取`P_B=T_B_E*T_E_C*P_C`换算结果，`Z`固定`-100mm`，夹爪世界绝对俯仰固定`-90deg`，成功启动后消费观察状态，callback 3只表示闭环抓取动作状态。
- 左右交替测试必须保持点位、抓取俯仰和速度严格镜像；左抓取显式使用A左profile，右抓取显式使用A右profile。
- q2硬件/脱困范围保持 `0..180 deg`，普通软件下限为`3deg`；当前专项路径Z为`-150 mm`。
- 不触碰用户修改的 `Engineer/MDK-ARM/Engineer.uvoptx`。
- 当前侧向专项测试绝对俯仰为`-5 deg`；用户确认ID1可接收控制值115，因此相对角上限同步为`+92.4 deg`。
- 当前完整上电顺序为 `q2+q3同步 -> q1 -> ID1/ID2`。
- 当前专项循环固定为 `左抓放 -> 回正 -> 右抓放 -> 回正 -> 重复`；仅在完整放置返回`DONE`后切换侧别。
- MG995测试期间临时切换到独立应用模式；PI6固定为TIM8_CH2右舵机，PI7固定为TIM8_CH3左舵机，两路上电目标均为90deg。
- 连续底盘速度接口使用`vx_mm_s`和`wz_rad_s`；正`vx`表示物理车头前进，正`wz`表示逻辑Yaw增加。`wz=0`且`vx!=0`时由IMU锁定进入直行时的航向，非零`wz`以上位机目标为主。
- 速度命令超时必须平滑停车并允许后续新命令恢复，不将一次上位机刷新中断锁存为底盘故障；急停和真实电机/IMU故障仍维持锁存保护。
- 阶段18只预留了固件内部接口；阶段19当时同步的生成协议哈希为`0x2588BA9A`，无需握手且非严格心跳；阶段29再次以用户更新的生成文件为线协议事实来源，当前哈希为`0x740E426B`。
- 新版生成解析器已自动回心跳和入站可靠消息ACK，固件运行层不得重复发送。
- 阶段26后默认模式已切到`APP_MODE_HOST_CONTROL`；MG995专项模式仍保留为显式可切换入口，HOST模式不自动运行AC/BD测试状态机。
- 阶段21当前应用模式保持`APP_MODE_ARM_TEACH_POINT`；BD观察参数只保存，不在本轮自动执行机械臂运动。
- 阶段22按用户确认切换到`APP_MODE_ARM_BD_OBSERVATION_TEST`；上电将执行HOME、左向底座预转、观察点直线运动并到位保持。
- 阶段23第一段不能继续只转q1；必须按实际关节插值和工具俯仰逐点证明`tool_center.y_mm <= 260mm`，且第二段仍通过完整笛卡尔预检。
- 阶段23最终过渡姿态固定为`q=[90,120,-48]deg`、ID1相对小臂`-18deg`；两段工具中心必须满足`|Y|<=260mm`，当前回放峰值为`231.470mm`。
- 阶段24右侧观测必须是阶段23左侧路径的严格Y/q1镜像：`q1=-90deg`、目标`Y=-57mm`，其余关节、俯仰、速度和安全限值不变。
- 阶段25必须永久保留左右两个BD观察点的独立宏，当前上传固件由`APP_ARM_BD_OBSERVATION_ACTIVE_SIDE`选择RIGHT；不得再通过覆盖同一组坐标来切换侧别。
- 阶段25当前上传版本固定ACTIVE_SIDE=RIGHT；LEFT/RIGHT两点均参与每次离线回放，但固件上电只执行活动侧，不能误解为上电自动连续测试两侧。
- 阶段26使用既有`APP_MODE_HOST_CONTROL`语义：启用USB协议、底盘、MG995和机械臂正常初始化，但不执行BD观察状态机；`VelocityCommand.linear_x`按m/s乘1000映射为mm/s，`angular_z`保持rad/s。
- 阶段27的HOST速度边界固定为`|linear_x|<=1.0m/s`、`|angular_z|<=1.5rad/s`、单轮`<=1.3m/s`，线/角加速度分别为`1.0m/s2`和`3.0rad/s2`；有限超限值逐字段钳位，NaN/Inf仍拒绝，300ms超时、零值死区和停稳判定保持不变。
- 阶段28的`task_id=2`开环完整抓放语义仅为历史实现；阶段44后HOST维护桥不再直接使用该语义，开环完整抓放保留为`app_arm_side_pick_place.c/.h`备选模块。
- 阶段44已启用`ArmTarget`闭环抓取，但仍不得把相机坐标直接当机械臂基座坐标执行；必须先通过外参、pose snapshot、年龄、IK、软件限位和路径预检。
- 阶段30的`430mm`限制只属于AC抓取终点到放置准备姿态，不修改通用工作空间边界；左右路径必须严格镜像，且必须按实际关节插值逐采样检查工具中心世界坐标`fabs(Y)<=430mm`。
- 阶段31保持阶段30的应用层专用约束，不修改全局工作空间边界；新抓取端点为`[0,+/-440,-140]mm`，抓后第一段waypoint工具中心目标高度约`-120mm`，第一段不得低于起点`-140mm`，完整两段必须满足`fabs(Y)<=440mm`且左右严格镜像。
- 阶段32相机固定安装在ID1之后并随夹爪俯仰；下位机继续使用`TOOL_CENTER`参考系，保存由ID1轴外参减去117mm工具长度得到的平移。阶段44为AC闭环联调已将`calibrated`改为1；若实机已知点验证不一致必须立即改回0。
- 阶段33只把AC单侧profile副本的运行时Y上限放宽为445mm；左右抓取终点仍为`Y=+/-440mm`，离线名义峰值仍为440mm，waypoint、Z约束和全局工作空间边界均不变。
- 阶段34已确认底座抽搐的源码根因：AC后方目标恰为`q1=+/-180deg`，到位误差按周期角计算且软件限位容差允许反馈到`+/-180.2deg`；随后释放位姿/释放后抬臂两次复制瞬时q1反馈，而关节轨迹插值把绝对q1再次`WrapTo180`，底层达妙位置目标却不做周期连续化，导致越界反馈被映射到另一端并形成约360deg目标跳变。本阶段不修改代码，后续修复必须同时处理后方边界和未指定q1保持语义，不能只改夹爪等待。
- 阶段35采用局部修复，不改全局周期角/绝对角轨迹语义：A左/A右后方目标固定为`+178/-178deg`，释放位姿使用`release_q_deg[q1]`，释放后抬臂使用`release_clearance_q_deg[q1]`；速度、Y/Z路径、ID1/ID2、BD观察和协议行为保持不变。
- 阶段35验证边界：`tools/arm_path_replay/run.cmd`和`Engineer/MDK-ARM/gcc_arm_check.cmd`均通过，`git diff --check`无空白错误；未运行Keil构建、未烧录、未做机械臂实机复测，并保持用户已有`Engineer.uvoptx`不受本轮工具改写。
- 阶段35的`+/-178deg`左右抓放流程作为AC开环备选方案永久保留；阶段36开始准备闭环方案，但本轮只确定并测试左侧相机观察姿态，通信协议和相机目标执行暂不修改。
- 阶段36将“末端竖直向下”解释为工具中心世界绝对俯仰`-90deg`；左侧方向固定`q1=+90deg`，工具中心X固定0、Y优先40mm，Z由当前运动学、ID1范围、工作空间和完整路径余量共同选择。
- 阶段36确认`[0,40,Z]mm/pitch=-90deg`在Z=180..320mm虽有三轴IK，但ID1需`-107.625..-130.401deg`，超过`-90deg`实机边界；不得通过放宽ID1软件限位强行使用该点。
- 阶段36最终左观察点为`[0,+105,110]mm/q1=+90deg/pitch=-90deg`，右侧保存为严格镜像；staging为`[+/-90,90,-80]deg`且ID1相对俯仰`-80deg`。完整回放`max|Y|=256.050mm<260mm`，左终点IK约`[90,125.436,-44.186]deg`、ID1约`-79.622deg`。
- 阶段36默认模式为`APP_MODE_ARM_BD_OBSERVATION_TEST`且活动侧LEFT；本轮不接入相机通信或视觉目标执行，不修改阶段35的AC开环备选、相机外参、协议或底盘。
- 阶段36验证边界：双侧BD与AC永久回放、ARM严格GCC（含HOST强制分支）和`git diff --check`通过；未运行Keil、未生成本轮AXF/HEX、未烧录或实机运动，且未触碰用户已有`Engineer.uvoptx`。
- 阶段37只调整BD观察点和BD离线验证上限；AC区不遵循BD的405mm上限，继续独立使用抓取终点`Y=+/-440mm`和抓后运行时上限445mm。
- 阶段37最终BD左右点为`[0,+/-400,110]mm`、绝对俯仰`-90deg`，当前活动侧仍为LEFT，staging仍为`[+/-90,90,-80]deg`；回放终点IK为`[+/-90,56.100,-112.631]deg`，BD峰值400/上限405mm。
- 阶段37验证边界：永久回放同时通过BD 400/405和AC 440/445，ARM严格GCC含HOST强制分支及`git diff --check`通过；未运行Keil、未生成本轮AXF/HEX、未烧录或实机运动，且未触碰用户已有`Engineer.uvoptx`。
- 阶段38最终BD左右点为`[0,+/-400,350]mm`、绝对俯仰`-45deg`，当前活动侧仍为LEFT；Y无需收小。终点IK约`[+/-90,69.663,-139.562]deg`，ID1相对俯仰约`-74.225deg`，距离`-90deg`下限约15.775deg。
- 阶段38只修改BD观察高度/俯仰及对应维护文档；staging、速度、BD专用405mm回放边界、AC `+/-440mm`抓取终点和445mm抓后约束均保持不变。
- 阶段38验证边界：永久回放同时通过BD 400/405和AC 440/445，ARM严格GCC含HOST强制分支及`git diff --check`通过；未运行Keil、未生成本轮AXF/HEX、未烧录或实机运动，且未触碰用户已有`Engineer.uvoptx`。
- 阶段39最终BD左右点为`[0,+/-200,350]mm`、绝对俯仰`-45deg`，当前活动侧仍为LEFT；终点IK约`[+/-90,114.050,-96.794]deg`，ID1相对俯仰约`-75.844deg`。
- 阶段39整段BD路径峰值为338.781mm而非终点200mm，这是HOME到staging的侧向展开所致；仍低于BD专用405mm边界。AC `+/-440mm`抓取终点和445mm抓后约束保持不变。
- 阶段39验证边界：永久回放、ARM严格GCC含HOST强制分支及`git diff --check`通过；未运行Keil、未生成本轮AXF/HEX、未烧录或实机运动，且未触碰用户已有`Engineer.uvoptx`。
- 阶段40优先值-30deg已通过，无需使用-35deg回退；BD点继续为`[0,+/-200,350]mm`，终点IK约`[+/-90,120.249,-87.711]deg`，ID1相对俯仰约`-57.960deg`。
- 阶段40整段BD路径最低Z为102.373mm、Y峰值为357.375mm，仍低于BD专用405mm边界；AC `+/-440mm`抓取终点和445mm抓后约束保持不变。
- 阶段40验证边界：永久回放、ARM严格GCC含HOST强制分支及`git diff --check`通过；未运行Keil、未生成本轮AXF/HEX、未烧录或实机运动，且未触碰用户已有`Engineer.uvoptx`。
- 阶段41最终采用用户选定的-53deg；BD点保持`[0,+/-200,350]mm`，终点IK约`[+/-90,110.456,-101.558]deg`，ID1相对俯仰约`-85.014deg`，距离-90deg下限约4.986deg。
- 阶段41整段BD路径最低Z为80.112mm、Y峰值为326.462mm，仍低于BD专用405mm边界；AC `+/-440mm`抓取终点和445mm抓后约束保持不变。
- 阶段41验证边界：永久回放、ARM严格GCC含HOST强制分支及`git diff --check`通过；未运行Keil、未生成本轮AXF/HEX、未烧录或实机运动，且未触碰用户已有`Engineer.uvoptx`。
- 阶段42不修改任何运动数值或协议实现，只固化参数所有权和下一步交接边界；当前默认仍为LEFT观察测试，协议更新待用户提供新版上下位机文件。
- 阶段43生成协议三件套继续定义哈希`0x740E426B`，未改变消息ID、结构体布局、CRC、可靠序号、握手或心跳；本轮只允许围绕文档中task 4/5语义适配维护层，不覆盖生成文件或BD运动参数。
- 阶段43 task 5以独立`Upper_Controller_Area_e`保存A/B/C/D，允许在其他离散动作运行时幂等更新；生成FSM先自动ACK，下一次桥任务再发送callback 5的执行中/完成。消费者通过`UpperControllerGetCurrentArea()`读取。
- 阶段43 task 4因没有二维码姿态坐标/关节值而只记录request/unsupported计数，不动作、不发送callback 4；当前协议也没有BD LEFT/RIGHT选择字段，task 5不会自动驱动观察点。
- 阶段43验证边界：永久机械臂回放、ARM严格GCC含HOST强制分支及`git diff --check`通过；未运行Keil、未生成本轮AXF/HEX、未做USB实机互通或二维码姿态运动，且未触碰用户已有`Engineer.uvoptx`。
- 阶段47最终AC/BD左右观察点保持`[0,+/-150,300]mm`，世界绝对俯仰改为`-58deg`；终点IK约`[+/-90,123.291,-84.168]deg`，终点ID1相对俯仰约`-85.459deg`，距离`-90deg`下限约4.541deg。
- 阶段47整段BD路径最低Z为77.557mm、Y峰值为318.051mm，仍低于BD专用405mm边界；AC `+/-440mm`抓取终点和445mm抓后约束保持不变。
- 阶段47验证边界：永久回放、ARM严格GCC含HOST强制分支及`git diff --check`通过；未运行Keil、未生成本轮AXF/HEX、未烧录或实机运动，且未触碰用户已有`Engineer.uvoptx`。
- 阶段48只修改AC闭环ArmTarget抓取俯仰宏和维护文档：`APP_ARM_AC_CLOSED_LOOP_PICK_TOOL_PITCH_DEG=-90deg`，`APP_ARM_AC_CLOSED_LOOP_PICK_Z_MM=-100mm`不变，AC开环`APP_ARM_POSTURE_TEST_TOOL_PITCH_DEG=-5deg`不变。
- 阶段48验证边界：ARM严格GCC含HOST强制分支、永久路径回放和`git diff --check`通过；ArmTarget运行时X/Y来自上位机坐标，具体点位可达性仍由收到目标后的IK/限位/路径预检决定；未运行Keil、未烧录或实机运动。

## Errors Encountered

| Error | Attempt | Resolution |
|---|---:|---|
| `apply_patch` path duplicated `Desktop/vison` | 1 | Patch tool is already rooted at the repository; use project-relative paths. |
| `rg` treated the `-460` search pattern as an option | 1 | Use `rg --` before patterns that begin with a dash. |
| `session-catchup.py` 无法由MSYS Python确定HOME | 1 | 已直接读取现有计划文件并核对Git/源码，不重复运行同一失败命令。 |
| Keil 链接报 `AppArmCommandIdInit/Next` 未定义 | 1 | 磁盘上的 `.uvprojx` 条目被旧Keil工程状态覆盖；重新把 `app_arm_command_id.c` 加入当前 target，并由Keil增量构建验证为0错误0警告。 |
| 文档残留搜索中的反引号触发bash命令替换并报EOF | 1 | 改用不含反引号的固定文字模式分组搜索，不重复原命令。 |
| PowerShell启动失败并报`8009001d` | 1 | 切换到原生`cmd.exe`执行检查和构建命令。 |
| `cmd`调用带显式引号的`rg.exe`路径时把引号当作文件名 | 1 | 该路径不含空格，后续直接使用未加引号的完整路径。 |
| `cmd`中的带空格搜索词 `BD LEFT coordinated` 被拆成多个参数 | 1 | 改用单符号搜索定位函数，再按行号读取上下文。 |
| `cmd`中的带空格搜索词 `int main` 被拆成多个参数 | 1 | 改用无空格符号 `main` 搜索，并按返回行号读取上下文。 |
| 本轮 PowerShell 启动再次报 `8009001d` | 1 | 按本机已验证策略切换到原生 `cmd.exe`，后续不混用 PowerShell/WSL 路径。 |
| `session-catchup.py` 再次因 MSYS Python 无法确定 HOME 失败 | 1 | 直接读取 `task_plan.md`、`findings.md`、`progress.md` 和 Git 状态恢复上下文，不再重试。 |
| 固定旧BD staging `[90,120,-48]deg` 扫描 `Z=180..320mm` 全部在直线路径中途无连续安全IK | 1 | 保持目标 `X=0/Y=40/pitch=-90deg`，改为联合搜索HOME可安全到达的新staging与目标Z，不放宽软件限位。 |
| 联合搜索 `q2=60..120deg/q3=-120..-40deg` 的HOME安全staging后，`Y=40/pitch=-90deg/Z=180..320mm` 仍无完整直线路径 | 2 | 分离检查目标终点IK、ID1和关节限位，确认是终点不可用还是近轴直线路径不可用，再决定最小几何调整。 |
| `[0,40,Z]mm/pitch=-90deg` 终点三轴IK可解但ID1需 `-107.6..-130.4deg`，超出 `-90deg` 下限 | 3 | 不放宽ID1实机边界；扫描最接近40mm且保留约10deg ID1余量的Y/Z组合，再做完整路径验证。 |
| 临时候选扫描器的 `target` 数组被后续局部 `target` 遮蔽，触发 `-Wshadow/-Werror` | 1 | 将数组重命名为 `scan_target`，保持严格告警检查后重跑。 |
| `cmd`中的多模式文档残留搜索再次把含空格和`|`的表达式拆成文件名 | 1 | 改为多个无空格字面量搜索分别执行，不重复该组合命令。 |
| 最终确认模式的带空格字面量搜索再次被`cmd`拆参 | 1 | 使用无空格符号`APP_MODE_ARM_BD_OBSERVATION_TEST`和源码行读取确认；严格编译已按默认模式通过。 |
| Phase 37残留搜索中的带空格固定词`260 mm`被原生`cmd`拆成文件名 | 1 | 使用无空格数值和旧点位分别搜索，已取得全部相关文档位置；不重复该命令。 |
| Phase 37最终AC宏组合搜索中的正则`|`被原生`cmd`解析 | 1 | 分别搜索两个无空格宏名，确认AC终点440mm、抓后上限445mm及profile引用均未改变。 |
| Phase 37最终阶段行展示搜索中的转义方括号被原生`cmd`当作路径 | 1 | 只影响展示性搜索；阶段37已由精确补丁写为`[complete]`，源码回放、严格编译和diff检查均已独立通过。 |
| Phase 38直接使用`[0,+/-400,350]mm/pitch=-90deg`时直线路径中途触发ID1下限 | 1 | 不放宽ID1限位；按用户允许改为斜向观察，继续验证俯仰候选。 |
| Phase 38使用`pitch=-60deg`时仍在接近终点处触发ID1下限 | 1 | 调整到`-45deg`后保留Y=400mm并通过双侧完整路径。 |
| `UV4.exe`增量构建未启动且未生成日志 | 2 | 两种等待方式均无构建产物更新后停止重复；保留GCC严格语法检查结果，并明确未取得本轮Keil构建结果。 |
| Node全路径统计脚本括号误写 | 1 | 修正`v[2]`后的括号后重新计算；该次失败未产生或修改工程文件。 |
| `session-catchup.py` 再次因MSYS Python无法确定HOME失败 | 2 | 这是现有环境限制；本轮已直接读取计划、进度、发现和Git状态，后续不再重复该命令。 |
| `cmd`下带空格的`rg`模式再次被拆成路径参数 | 1 | 改用不含空格的符号搜索和定点源码读取；未产生文件或源码修改。 |
| 镜像回放新增seed变量触发`-Wshadow` | 1 | A区点位检查的局部seed与全路径seed同名；改为`left_point_seed/right_point_seed`后重跑严格检查。 |
| `cmd`将含空格和`->`的组合文档搜索模式拆成参数 | 1 | 改为逐个固定短语与坐标搜索；搜索失败未产生文件。 |
| `cmd`把未转义的正则`|`解释为管道 | 1 | 后续使用单个固定符号逐项搜索，不再在原生cmd中使用该组合表达式。 |
| Phase 34定点读取中带空格的`rg`模式被原生`cmd`拆分 | 1 | 改用不含空格的单符号搜索与上下文输出；只读失败未修改源码。 |
| Phase 34最终展示性搜索的带空格模式再次被`cmd`拆分 | 1 | 不重复该命令；阶段状态和内存行号均已有前序无空格搜索证据，`git diff --check`已通过。 |
| Phase 35旧文档组合残留搜索被`cmd`按`|`拆分 | 1 | 改为逐个无空格关键词和限定文档搜索；只读失败未修改文件，其他验证结果不受影响。 |
| Phase 36内存索引组合搜索被`cmd`按`|`拆分 | 1 | 停止组合模式，后续用单符号搜索实时源码；该只读失败未修改工程文件。 |
| 删除HOLDING死状态时把`.c`上下文放入`.h`补丁块 | 1 | 补丁原子校验失败且未修改文件；拆成两个文件更新块重新执行。 |
| 最终计划行的带空格正则在`cmd`中被拆开 | 1 | 改用简单数字固定搜索确认第13阶段；不影响计划内容或源码。 |
| 右侧诊断的首个组合符号搜索在`cmd`中被`|`截断 | 1 | 改用单个函数名逐项搜索和定点读取，不重复组合命令。 |
| 搜索误包含不存在的`APPLICATION/arm/arm_host.c` | 2 | host实现位于现有arm模块文件而非该路径；后续只搜索已确认存在的`arm.c/arm_trajectory.c/arm_host.h`。 |
| 临时右侧复现由GCC在`C:\WINDOWS`创建中间文件失败 | 1 | 权限导致且诊断源码未执行；下一次加入`-pipe`避免该临时文件路径，不重复原命令。 |
| 临时复现加入`-pipe`后链接阶段仍访问`C:\WINDOWS` | 2 | 使用临时批处理在子进程内显式设置`TEMP/TMP`到用户临时目录，再调用同一编译器。 |
| 从仓库根目录调用`gcc_arm_check.cmd`找不到`../APPLICATION` | 1 | 脚本使用相对当前目录的源码路径；切换到`Engineer/MDK-ARM`后重跑，不重复根目录调用。 |
| `cmd`把包含多个`|`的残留正则拆成管道 | 1 | 改为逐个固定符号搜索；该次失败未修改任何文件。 |
| 多文档补丁因`HANDOFF.md`验证段落上下文不匹配而拒绝 | 1 | 补丁原子校验失败且未改文件；改为按现有行拆分小补丁。 |
| 第二次跨文件文档补丁仍被`HANDOFF.md`后续行阻断 | 2 | 停止跨文件补丁；每个文档只匹配单条稳定锚点并独立更新。 |
| 最终两个带空格/转义的行号搜索被`cmd`拆成参数 | 1 | 改用无空格符号名定位；严格编译、回放和diff检查结果不受影响。 |
| 原生`cmd`调用MSYS `sed -n`时引号被错误解析 | 1 | 该批只读命令未读取或修改源码；改用`rg -n ^`输出完整文件，不重复相同命令。 |
| `rg -n ^`在原生`cmd`中把工程路径误作正则 | 1 | `^`被shell处理后参数位置改变；改用`rg -n . <file>`完成读取，不重复原命令。 |
| 原生`cmd`拆开Python `-c`的带空格脚本并报字符串未结束 | 1 | XML未被修改；改用精确`FileName`文本计数和上下文核对工程条目。 |
| 最终行号搜索中的正则`|`和转义方括号再次被原生`cmd`拆分 | 1 | 仅影响展示性定位；改用已取得的单符号行号，不影响编译、回放或源码结果。 |
| Phase 28首次跨文件文档补丁因TUNING_GUIDE上下文过时而原子拒绝 | 1 | 无文件被修改；拆为协议/概要文档和精确TUNING_GUIDE段落两个补丁后成功应用。 |
| Phase 28最终三个带空格/或号的展示性`rg`模式被cmd拆分 | 1 | 仅展示搜索失败；改用定点读取和单符号结果确认，构建、MAP和diff验证不受影响。 |
| MG995首次跨文件补丁在Keil XML段落含多余`@@` | 1 | `apply_patch`原子拒绝且没有文件被修改；拆分为驱动、应用和工程三个小补丁。 |
| Phase 29首次跨文件文档补丁因HANDOFF换行上下文不匹配而原子拒绝 | 1 | 没有文件被修改；改为逐文件、使用当前稳定锚点的小补丁。 |
| Keil重建自动同步`Engineer.uvoptx`并删除旧IDE条目 | 1 | 构建前该文件干净；逐项恢复被Keil改写的条目和编号，最终`git diff -- Engineer.uvoptx`为空。 |
| 首次恢复`Engineer.uvoptx`的逆向补丁因倒序hunk无法匹配 | 1 | 补丁原子拒绝且未修改文件；按文件正序应用精确块后恢复成功。 |
| FreeRTOS优先级搜索使用了不存在的include路径 | 1 | 只读搜索未修改文件；共享TIM14时基已从实时timebase和ISR源码完成核对，不重复错误路径。 |
| `git diff --check`发现`Engineer.uvprojx`末尾新增空行 | 1 | 删除且只删除XML结束标签后的多余空行，工程条目内容保持不变。 |
| GCC检查`main.c`进入Keil RVDS FreeRTOS端口后内联汇编不兼容 | 1 | TIM/GPIO/ISR在此之前均已通过；从GCC辅助列表移除`main.c`，改用原生Keil构建验证真实编译器路径。 |
| 首次Keil构建仍报`Failed to create temporary file name`且`Target not created` | 1 | 不能采信日志末尾的0错误；按本机专用流程改用`MDK-ARM/Engineer/tmp`并通过MSYS `env.exe`执行完整rebuild。 |
| 最终多符号行号搜索再次被原生`cmd`的`|`拆分 | 1 | 只影响展示命令；改用各单符号结果和map调用关系完成核对，源码与构建不受影响。 |
| `findstr`的带空格完成状态模式仍被`cmd`拆分 | 1 | 停止该展示性搜索；阶段状态由刚应用的计划补丁确定，不再重复。 |
| 本轮首次内存索引的带空格`rg`模式被`cmd`拆分 | 1 | 改用无空格`Damiao`符号搜索并取得相关索引；未影响源码。 |
| `planning-with-files`完成检查把旧格式`[complete]`识别为未完成 | 1 | 现有18个阶段均使用历史格式且阶段18已明确标记complete；不为满足辅助脚本批量改写计划历史，实际完成状态以计划行、progress和验证结果为准。 |
| 本轮`session-catchup.py`仍因MSYS Python无法确定HOME失败 | 1 | 直接读取现有计划、发现和进度文件继续；这是已记录的环境限制，本轮不再重复该命令。 |
| AC waypoint临时搜索器首次GCC编译尝试写`C:\\WINDOWS`临时文件失败 | 1 | 源码未执行；下一次在同一cmd子进程把`TEMP/TMP`显式设为`tools/arm_path_replay/_tmp`后再编译。 |
| 临时搜索器把返回`void`的`ArmForwardKinematics3DOF()`用于布尔判断 | 1 | 改为先调用FK写入腕点，再以`ArmToolGetCenterFromWrist()`返回值和有限性完成校验。 |
| 最终内存引用行号的带空格/或号`rg`模式再次被原生cmd拆分 | 1 | 只影响引用定位；改用无空格单一正则逐项查询，不重复失败命令，不影响源码和验证。 |
| Phase 31恢复上下文时`session-catchup.py`仍因MSYS Python无法确定HOME失败 | 1 | 已完整读取现有计划、发现和进度并使用实时源码继续；这是已知环境限制，本轮不再重复该命令。 |
| Phase 31首次跨文件文档补丁因`FRUIT_TASK_FLOW.md`换行上下文不匹配而拒绝 | 1 | `apply_patch`原子拒绝，确认无文档改变；改为读取实时段落后逐文件应用小补丁。 |
| 最终内存/计划定位搜索中的组合正则再次被原生cmd拆分 | 1 | 该批只读搜索未影响任何文件或验证结果；改用单个无空格符号逐项定位，不重复组合表达式。 |
| 本轮首次内存搜索由PowerShell启动失败并报`8009001d` | 1 | 立即切换原生`cmd.exe`，后续检查不再使用PowerShell。 |
| 阶段21首次Keil重建使用了`Engineer/tmp/-r/-j1`并长期无日志 | 1 | 只结束本次启动的子进程，保留用户原Keil窗口；从当天成功会话提取并改用`MDK-ARM/tmp/-cr/-j0`准确命令。 |
| 原生`cmd`把带正则或号的两个`rg`命令拆成管道 | 1 | 改用单个固定符号逐项搜索；只读失败未修改工程文件。 |
| Phase 32首次相机配置/文档跨文件补丁因`HANDOFF.md`实时措辞不匹配而拒绝 | 1 | `apply_patch`原子拒绝且未改文件；读取实时段落后拆成配置和逐文档独立补丁。 |
| Phase 33首次跨文件记录补丁因`findings.md`预期措辞不存在而拒绝 | 1 | `apply_patch`原子拒绝且未产生部分修改；读取各记录文件实时尾部后分别更新。 |
| Phase 34两个带空格函数声明搜索被原生cmd拆成文件名 | 1 | 只读搜索未修改文件；后续改用无空格函数符号和精确声明正则，不重复原命令。 |
| 新增上位机模式的严格GCC检查报`now_ms`赋值后未使用 | 1 | 在该模式的`AppArmTask`分支显式标记时间参数已消费；自动测试模式仍继续使用更新时间。 |
| 阶段23只读搜索再次包含不存在的`arm_host.c` | 1 | 立即停止该路径；主机命令实现继续从已确认存在的`arm.c/arm_trajectory.c/arm.h`读取，不影响源码。 |
| 阶段23临时候选搜索首次GCC编译向`C:\\WINDOWS`创建临时文件失败 | 1 | 改用MSYS `env.exe`在单个子进程中把TEMP/TMP显式指向`tools/arm_path_replay/_tmp`，不重复原命令。 |
| 临时候选搜索引用了`arm_trajectory.c`私有的1deg采样宏 | 1 | 搜索工具显式定义等值`SEARCH_JOINT_STEP_DEG=1.0f`，保持算法一致且不扩大正式头文件接口。 |
| 阶段23首次技能/内存读取的`cmd`引号被包装层拆分，随后PowerShell又报`8009001d` | 1 | 使用原生cmd和不带空格的字面路径逐项读取；未修改文件，后续不再使用失败命令形态。 |
| 阶段23首次跨文件正式补丁含空的hunk终止标记 | 1 | `apply_patch`原子拒绝且未改文件；拆为配置/运行时、回放和记录三个小补丁后成功应用。 |
